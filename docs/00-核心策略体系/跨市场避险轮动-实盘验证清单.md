# 跨市场避险轮动 — 实盘验证清单

> 2026-09-12 | 适用：`rotation/cross_market_momentum_strategy.py` 上线实盘前的逐项验证。
> 配套：`docs/06-标准规范/07_策略运行分析标准流程.md`（日终通用检查）。

---

## 〇、为什么要单独验证 —— 组合的验证记录不能背书

此前在「攻防一体化」组合（高波动7.1 + 熊市防守-01）中验证过一段时间，**系统链路是通的**。
但那份记录**不覆盖本策略**，原因有三：

| 层面 | 攻防组合验证 | 本策略 |
|:---|:---|:---|
| 系统链路（数据同步→信号落库→人工确认→成交录入→持仓回显） | ✅ 覆盖 | ❌ **不用重验**（通用） |
| 组合资金分配（regime 权重 → `allocated_capital` → `context.total_assets`） | ✅ 覆盖 | ⚠️ 单独跑**没有这一层** |
| **策略自身状态机**（F1~F9） | ❌ **未覆盖**（代码不同） | ✅ **必须验** |
| **B1：无组合时的权益同步** | ❌ 组合模式掩盖了它 | ✅ **必须验** |

> **一句话：验证过的是「系统能跑」，没验证过的是「这个策略 + 这种跑法」。**

---

## 一、前置配置检查（不通过不要启动）

| # | 检查项 | 期望 | 怎么查 |
|:---|:---|:---|:---|
| P1 | 策略 `run_mode` | `live` | 策略详情页 |
| P2 | 策略 `status` | `running` | 同上 |
| P3 | 策略 `account_id` | **已绑定**（实盘必填） | 同上 |
| P4 | 该 `account_id` 未被其它**活跃组合**引用 | 无冲突 | 账户列表 / 组合列表 |
| P5 | 账户 `total_balance` > 0 | 是 | 账户详情（为 0 时 B1 同步会跳过） |
| P6 | 资金规模 ≥ 5 万 | 建议 ≥10 万 | 见 §三 国债腿取整<br>⚠️ **口径澄清（2026-09-21）**：该下限**由 §三 取整损耗推出**，指**资金规模到位后（≈ S2 量级）的目标态**；<br>**S1 验证期按 §三「有意接受」放宽**（小资金 + 只看行为，不看收益）—— 见 §六 ③ |
| P7 | 历史持仓与 DB `positions` 一致 | 是 | 人工核对 |

---

## 二、验证项清单

> 每项：**怎么造场景 → 看什么 → 通过标准**。建议每项都留一份日志片段作为证据。

### V1 — 重启不丢持仓、不重复建仓 🔴
- **造场景**：启动策略并持有一笔 → 停策略 → 重启 → 触发一次日终驱动
- **看**：日志是否出现 `恢复持仓 N 只`；是否对**已持有**标的重复发买入信号
- **通过**：`_holdings` 从 DB 正确重建，无重复建仓信号
- **关联**：F1（旧代码在实盘会每日抹掉持仓）

### V2 — T+1 守卫不误锁 🔴
- **造场景**：连续运行 ≥3 个交易日
- **看**：是否每个调仓日都无卖出信号（= 被永久锁死）；换仓是否按时发生
- **通过**：正常换仓，无「持有 X 天却一笔不卖」
- **关联**：F2（我曾把 `fill_date` 补为当日，导致实盘永久冻结；已修正）
- **⚠️ 这是最容易复发的一项**：`_restore_positions_from_db` 每日重建 `_holdings`，
  任何"补 fill_date"的改动都会立刻冻结策略

### V3 — 退出信号能重发 🟠
- **造场景**：制造一次「卖出信号发出但未成交」（如人工不执行）
- **看**：`exit_retry_days`（默认 3）个交易日后是否**重发**卖出信号
- **通过**：重发；而不是永久沉默
- **关联**：F3（旧代码 `_exit_pending` 是单向阀）
- **本轮执行记录（2026-09-21 起，进行中）**：场景**自然就位** —— 21:38 的 `159985.SZ` 卖出信号状态为 `pending_manual` 且**未执行**（= 清单要求的"信号发出但未成交"）。
  **处置：不执行、不取消**，等它重发以留痕。
  - **预计重发日**：**2026-09-24** 日终（`exit_retry_days=3`，按 `_rebalance_seq` 计 3 个调仓日：09-22 → 1、09-23 → 2、09-24 → 3 ✓）
  - ⚠️ **若进程中途重启则更早**：`_exit_pending` 是**内存态**，重启即清空 → 下次驱动**立刻**重发
  - **观察点**：① 重发数量是否仍取 **broker 实际持仓**（`7700`，而非策略自维护 `shares`）② 是否**只新建一条**信号（不重复登记/不改旧行）③ 旧信号是否仍是 `pending`（会累积 → 需人工 `cancelled` 收口）④ 告警是否被 `alert_manager` 判重而**不推送**（"没推送 ≠ 没信号"）

### V4 — 止损后不被买回 🟠
- **造场景**：标的跌破入场价 ×0.92
- **看**：止损卖出成交后，次日是否被重新选为目标并买回
- **通过**：不买回（至少不在同一轮）
- **关联**：F4

### V5 — 脏数据不崩 🟠
- **造场景**：数据源出现 NaN / 0 价（可手工插入一条脏 bar 到 DB）
- **看**：日志是否出现 `丢弃非法 bar`；当日是否仍正常出信号（而非整体静默）
- **通过**：该标的被跳过，其余标的正常；无 `int(nan)` 类异常
- **关联**：F5

### V6 — 停牌标的不入选 🟠
- **造场景**：某池内 ETF 停牌（`volume=0`）
- **看**：该标的是否仍被选为目标
- **通过**：被排除
- **关联**：F6

### V7 — 数据缺失不误切国债 🔴
- **造场景**：**故意让某日 ETF 行情整体缺失**（如跳过当日 ETF 同步）
- **看**：日志是否出现 `候选池无任何标的带当日(...)行情，判定为数据缺失，跳过本次调仓`；
  是否**没有**发出「卖出持仓 + 买入国债」的信号
- **通过**：当日不动仓，次日数据恢复后正常
- **关联**：F9（否则会白付两轮换手，且期间止损判据不可信）
- **⚠️ 这是最值得实测的一项**：不造场景几乎不会自然遇到，但一旦遇到损失明确

### V8 — sizing 基准随权益变动 🔴
- **造场景**：让账户权益发生明显变化（如人工调整余额，或经历一段涨跌）
- **看**：次日建议股数是否随之变化；日志是否出现 `sizing 基准同步: 策略=... 账户=... 权益 X → Y`
- **通过**：建议股数 ∝ 账户权益；不再恒定
- **关联**：**B1**（旧代码 `context.total_assets` 启动后永不更新）
- **⚠️ 这是单独跑与组合跑的唯一实质差异**

### V9 — 成交录入 → 次日可见 🟠
- **造场景**：人工确认成交并录入
- **看**：次日驱动时策略的持仓/成本是否与 DB 一致
- **通过**：一致
- **关联**：框架链路（组合阶段已验证，此处只做回归确认）

### V10 — 重启后不被多锁（B4 已修，需回归确认）🟠
- **造场景**：持有持仓 → 重启 → 连续观察 ≥2 个交易日
- **看**：日志是否出现 `重启恢复: 策略 ... 恢复了 N 只持仓的 _held_days`；
  重启后的第 1~2 个交易日是否就能正常轮动
- **通过**：有 `_held_days` 恢复日志；**不出现「连续多日不换仓」**
- **关联**：B4（修复 `34354f6`）
- **⚠️ 未修复时的后果比想象严重**：`_held_days` 重启归零 → `min_hold_days=3` 守卫
  把持仓当「刚买入」→ 重启间隔 **< 3 个交易日**时计数永远到不了阈值 →
  **策略永不轮动**（止损仍有效，但丧失轮动这条主要 alpha）。
  **验证期重启频繁，这一项必须看**，否则会把「策略不动」误判成信号/数据问题

### V11 — 卖出用 broker 实际数量 🟠
- **造场景**：让一笔买入被部分成交/缩减（如资金不足）
- **看**：卖出信号的数量是否等于实际持仓
- **通过**：不会因「可卖数量不足」被拒 → 资金正常释放
- **关联**：`cross-market-cash-bug-chain`（整条资金死锁链）

---

## 三、已知不验证项（有意接受）

| 项 | 说明 |
|:---|:---|
| **收益/回撤绝对值** | 小资金下取整损耗大，与回测不可比。验证期只看**行为是否正确**，不看收益 |
| **国债腿仓位比例** | 511010 单价 141 元 → 2 万账户只能建 **68.7%** 仓（设计 90%，损耗 23.7%）。<br>资金到 **10 万** 时损耗降到 **5.9%**，可接受，**不需要改代码** |
| 组合层面配比 | 本策略单独跑，不参与组合资金分配 |

---

## 四、缺陷索引（本清单对应的修复）

| 编号 | 缺陷 | 提交 |
|:---|:---|:---|
| F1 | 实盘 `_reconcile_holdings` 每日抹掉持仓（阻断） | `f45f233` |
| F2 | 恢复持仓 `fill_date` 处理（曾引入冻结回归） | `f45f233` / `c93ede3` |
| F3 | `_exit_pending` 单向阀 → 持仓冻结且止损失效 | `f45f233` |
| F4 | 止损后次日买回 | `f45f233` |
| F5 | NaN 无防护（静默关止损） | `f45f233` |
| F6 | 停牌标的通过量比过滤 | `f45f233` |
| F7 | `get_daily_diagnostic` 裸异常 | `f45f233` |
| F8 | 四大模块契约函数与主流程分叉 | `f45f233` |
| F9 | 数据整体缺失时误切国债 + 陈旧价误止损 | `ac36a1a` |
| B1 | 非组合 live 策略 sizing 基准永不更新 | `f506283` |
| B4 | 重启后 `_held_days` 归零 → 重启频繁时永不轮动 | `34354f6` |

---

## 五、验证状态（2026-09-19 核查）

> **本节范围仅限本清单（cross_market 专属的 P1–P7 / V1–V11）。**
>
> ⚠️ **不要把它读成"本项目没做过实盘验证"** —— 本项目**做过**，见下"已完成的验证"。
> 本节的结论只是：**这份清单对应的验证，尚未留痕。**
>
> ### 已完成的验证（供对照，勿与本节混淆）
>
> | 层面 | 证据 |
> |:---|:---|
> | **系统链路**（数据同步→信号落库→人工确认→成交录入→持仓回显） | 本文件 §〇 自述：曾在「攻防一体化」组合中验证过一段时间 |
> | **策略级（2026-07 那批）** | `docs/04-归档/策略历史/进攻防御双策略体系验证报告-2026-07.md`（低吸 v6.9 + 多资产 v1.5.2，含预注册测试） |
>
> ⚠️ **但需注意对象错位**：那份报告的**防御腿「多资产趋势轮动」已于提交 `264aa32` 删除**；
> 当前主池是 **高波动 v7.1 + 熊市防守-01**（`高波动动量轮动策略迭代记录.md:16` 标 v7.1 为"实盘确认版本"，
> 但**未找到当前这批策略的独立策略级验证报告**）。

> ✅ **本清单已完成首次完整执行（2026-09-19），结果见
> `docs/00-核心策略体系/跨市场避险轮动-实盘验证记录-2026-09.md`。**
>
> 概要：**V1 / V8 / V10 三项已实际验证通过**（含 🔴 的 B1、B4 两个修复）；
> V2 无法判定（需与回测同期信号对照）；**V3–V7 / V9 / V11 共 7 项场景未发生，需主动构造**。
>
> ⚠️ **方法提醒（本轮教训）**：证据在**全量日志 `quant_server.log*`** 里，
> **不在 `strategy_decision.log`**（后者经 `LiveDecisionFilter` 过滤，不含运行时字段恢复类日志）。
> 首次查询因只查决策日志，一度误判 V8/V10 为"未验证"。

> ⚠️ **仍待办**：实盘运行时长（5 个交易日）未达 **S1 小资金实盘**门槛（**≥1 个月**；2026-09-19 由原「S1 纸上交易 ≥20 交易日/≥10 笔」+「S2 ≥1 个月」**合并**）；
> 且 **5 天 0 信号**，故"等够天数"并非有效路径 —— 需主动构造场景。

| 项 | 实测 | 判据 | 状态 |
|:---|:---|:---|:---|
| 实盘运行时长 | `v1.0-实盘` 2026-09-12 11:37 → 09-19 05:49（7 自然日）；`2.0` 2026-09-13 10:54 → 运行中 | — | — |
| 净值记录（`strategy_daily_performance`） | **6 天**（2026-09-14 ~ 09-21，2026-09-21 更新） | — | — |
| **S1 小资金实盘门槛**（`06_策略实盘准入标准` §三，2026-09-19 由 S1+S2 合并） | **6 个交易日**（2026-09-21 更新） | **≥ 1 个月** | ❌ **未达** |
| P1–P7 前置配置检查 | **5 ✅ / P6 ❌ / P7 待人工**（2026-09-21 逐项实测，见下） | 逐项可查 | 🟠 **部分** |
| V1–V11 验证项结果 | **3 项实测留痕 + 1 进行中 + 2 部分 + 4 未触发 + 1 待人工**（2026-09-21 逐项实测，见下） | 每项留日志证据 | 🟠 **部分** |
| G5 准入裁决（签字 + 版本冻结） | 无 | `06_策略实盘准入标准` §二 | ❌ 未做 |

### 2026-09-21 复测留痕（逐项实测）

> 口径：P 组查生产库 `quant_signals`（2026-09-21）；V 组扫全量日志 `quant_server.log*`（09-14 ~ 09-21，8 个文件）
> —— **遵循上面的方法提醒：证据在全量日志，不在 `strategy_decision.log`。**

**P1–P7（前置配置，5 ✅ / 1 ❌ / 1 待人工）**

| # | 要求 | 实测 | 判定 |
|:--|:---|:---|:---|
| P1 | `run_mode=live` | `live` | ✅ |
| P2 | `status=running` | `running` | ✅ |
| P3 | 账户已绑定 | `e3b7d893`（同账户无其它 running 策略） | ✅ |
| P4 | 该账户未被其它**活跃组合**引用 | active 组合「攻防一体化」绑 **`84d81a14`**（另一账户），成员 `dc862847`/`cebe247d` → 无冲突 | ✅ |
| P5 | `total_balance > 0` | **19,873.24** | ✅ |
| **P6** | **资金 ≥ 5 万**（建议 ≥10 万） | **19,873.24** | ❌ **未达**（见 §三 国债腿取整：2 万账户只能建 68.7% 仓） |
| P7 | 历史持仓与 DB `positions` 一致 | 内部三方一致（DB `159985.SZ 7700@2.31` ↔ 策略恢复 `7700` ↔ 账户市值 17,663.80 = 7700×2.294）；**与券商实际持仓的一致性系统看不到 → 待人工核对** | ⚠️ 待人工 |

**V1–V11（验证项，3 留痕 / 1 进行中 / 2 部分 / 4 未触发 / 1 待人工）**

| # | 项 | 本轮实测 | 判定 |
|:--|:---|:---|:---|
| V1 | 重启不丢持仓 | **63 次**「从 DB 恢复了 1 只股票的持仓」（当日 3 次重启，`7700` 股始终一致） | ✅ 留痕（复现 09-19 结论） |
| V2 | T+1 守卫不误锁 | 需"换仓按时发生"—— 实盘 6 个交易日**尚无一次完成的换仓**（唯一持仓 09-14 建仓后未换） | ❌ 未触发 |
| V3 | 退出信号能重发 | **场景已自然就位**：`159985` 卖出 `pending_manual` **未成交** = 清单要求的场景；`exit_retry_days=3` 个调仓日内应重发 | ⏳ 进行中 |
| V4 | 止损后不被买回 | 需"跌破入场价×0.92"——**实盘止损 0 次**（当前 −0.69%） | ❌ 未触发 |
| V5 | 脏数据不崩 | **3 次**「丢弃非法 bar: `158008.OF` close=0」+ 进程继续（09-19 时尚无此事例） | ✅ 留痕（**新增**） |
| V6 | 停牌标的不入选 | 需停牌标的出现 —— 0 次 | ❌ 未触发 |
| V7 | 数据缺失不误切国债 | 需"候选池当日行情整体缺失" —— 0 次 F9 触发；<br>⚠️ 但 2026-09-21 已在**更上游**加了框架级门禁（`main_engine` 完整性门扩展至 `index_daily`/`etf_daily`） | ❌ 未触发（防护已加强） |
| V8 | sizing 基准随权益变动 | **18 次**「sizing 基准同步 … 权益 19912 → 19873（可用 2209）」 | ✅ 留痕（复现 09-19 结论） |
| V9 | 成交录入 → 次日可见 | **需人工录一笔成交** | ❌ 待人工 |
| V10 | 重启后不被多锁 | 当日 3 次重启后策略仍正常产生信号（未被 `min_hold_days` 锁死） | ⚠️ 弱留痕（缺对照观测点） |
| V11 | 卖出用 broker 实际数量 | 今日卖出 `7700` == broker 持仓 `7700` → 账实一致，但**两者相等时无法证明"取的是 broker 数量"**；需"broker 缩减过数量"的不等场景 | ⚠️ 部分 |

**两点必须说清：**

1. ⚠️ **§四 的 F1~F9 / B1 / B4 是「代码已修」的记录，不是「验证通过」的记录。**
   V1–V11 要验的正是「修完之后，在真实运行里行为对不对」「本清单末行原文」——
   而那部分**没有任何留痕**。
2. ⚠️ **本清单末行原文**：*「每项验证通过后在 CI/人工记录中留痕」* ——
   **本项目无 CI**（`06_策略实盘准入标准` §六 已登记该缺口），**人工留痕亦未建立**。

**因此**：cross_market 目前的状态是「**有完整回测 + 一份完整验证清单 + 6 天实盘（2026-09-21 更新）**」，
**按本项目自己的标准，它尚未完成实盘验证**。

---

## 六、资金路径（2026-09-21 新增）

> 依据：§一 **P6**（资金 ≥5 万 / 建议 ≥10 万）+ §三（国债腿取整）。
> **目的：让策略能按设计执行** —— 与策略逻辑无关，不改变任何 `DEFAULT_PARAMS`。

**① P6 现状**：账户 `e3b7d893` = **19,873.24 < 50,000** → ❌ 未达（§五 2026-09-21 实测）。

**② 取整是"阶梯式"的 —— 落点比金额更重要**

`511010` 一手 = 100 股 × **140.67** ≈ **1.41 万**；设计仓位 90%；口径 `floor(资金×90% ÷ 14,067)` 手：

| 资金 | 可建手数 | 实际仓位 | 相对 90% 损耗 | |
|:---|---:|---:|---:|:---|
| 2 万（现） | 1 | 70.3% | **−21.9%** | ✗ |
| **5 万** | 3 | 84.4% | **−6.2%** | ✅ 甜点 |
| 6 万 | 3 | 70.3% | −21.9% | ✗ |
| 7 万 | 4 | 80.4% | −10.7% | |
| **8 万** | 5 | 87.9% | **−2.3%** | ✅ 甜点 |
| 9 万 | 5 | 78.1% | −13.2% | |
| 10 万 | 6 | 84.4% | −6.2% | |
| 20 万 | 12 | 84.4% | −6.2% | |
| **30 万** | 19 | 89.1% | **−1.0%** | ✅ 甜点 |

> ⚠️ 手价随行情变动（现 `511010` = 140.67）；资金落点应在**下单前按最新价重算一次**。

**③ 路径（目标仓位已定：30 万 —— 决策人 2026-09-21）**

| 阶段 | 金额 | 依据 | 前置 |
|:---|:---|:---|:---|
| **S1 验证期（当前）** | **维持 2 万**（**不补资金**） | §三 已把「小资金取整损耗」列为**有意接受**：**验证期只看行为是否正确，不看收益**。此时补资金＝**改变被验证对象**（sizing 与取整动态都变），与"验证策略可靠、确保实盘稳定"的目的相悖 | — |
| **S2（进入时一次到位）** | **30 万**（= 目标仓位 100%） | `06` §三 S2「按设计资金分配」；同时满足 P6（≥5 万，建议 ≥10 万） | 五条判据全满足：① 满 ≥1 个月（09-14 起 → ≈**2026-10-14**）② 判据 4 有一段**无未预期缺陷**观察期 ③ G5⑥ 清单补完（6 项造场景 + V9 录单 + P7 核对）④ G5⑦ **独立复审记录**（`strategy-reviewer`）⑤ G5⑧ 版本冻结 |

> ⚠️ **与 §一 P6 的口径澄清（2026-09-21）**：P6 写「资金 ≥ 5 万」且末列标注「**见 §三 国债腿取整**」
> —— 即该下限**由取整损耗推出**，指的是**资金规模到位后（≈ S2 量级）的目标态**；
> **S1 验证期按 §三 有意放宽**（小资金 + 只看行为）。两份条款不冲突，但**读时须并读**。

> 📌 **上表的「取整甜点」在 S2 落地时用于选点**（不是 S1 的动作）：`511010` 一手 = 100 股 × 140.67 ≈ **1.41 万**，
> 设计仓位 90%，口径 `floor(资金×90% ÷ 14,067)` 手 —— **30 万 → 19 手 / 89.1% 仓 / 损耗 −1.0%**，正好是甜点。
> 若 S2 资金将来调整，参考下表的"阶梯"（6 万只能 3 手 −21.9%、9 万 5 手 −13.2%，**8 万** 5 手 −2.3%）。

**④ 两条操作注意**

1. **S1 观察起点无需重记**（2026-09-21 随路径修正）—— 既然 S1 **不补资金**，就不存在"sizing 基准跳动"的问题：观察期起点固定为 **2026-09-22**（理由见 `08_实盘策略报告/实盘执行监控-2026-09-22起.md` §二 判据④）。
2. ⚠️ **不要把资金塞进 active 组合「攻防一体化」** —— P4 实测该组合绑的是**另一账户** `84d81a14`，成员两腿均已 `stopped`；资金进去会被分给停掉的腿。

---

*本清单随策略代码变更同步更新；每项验证通过后在 CI/人工记录中留痕。*
