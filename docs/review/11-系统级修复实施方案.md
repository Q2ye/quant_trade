# 系统级修复实施方案（基于 A/B/C/D 风险四分类）

> 2026-08 | 前置文档：`00-总报告.md`（223+ 项问题）→ `10-review报告校准说明.md`（半自动模式校准）
> **总纪律**：小步提交（每项独立 commit 可独立 revert）｜修前先立测试基线｜C 类逐项回测对账｜D 类逐项引用验证｜**录单主链路（trade_record_service 7 步事务）冻结不动**
> 目标：修 bug 与加防御不引起功能问题；改口径与删代码严格验证后分批执行。

---

# 第一部分：四分类清单（60 项 P0 + 关键 P1 的最终归类）

## ✅ A 类：修 bug（低风险——修完更正确，不改业务语义）

| # | 问题 | 位置 | 一句话修复方案 | 验证方法 |
|:---|:---|:---|:---|:---|
| A1 | 🔴 delete 条件被丢弃→无条件全表删除 | `hyper_repository_base.py:250` | `query = query.where(...)` 一行修复 | 单测：delete_by_time_range 只删区间行 + 全库 grep 调用点审计 |
| A2 | 🔴 RedisCache 序列化恒 TypeError | `redis_cache.py:68-73` | 整个 entry 走 pickle/msgpack，不塞 JSON | 单测：set/get roundtrip；启用 Redis 后冒烟 |
| A3 | parent_task_id 缺列（批量同步全链路失败） | `create_table.sql:1396-1415` | `ALTER TABLE data_sync_tasks ADD COLUMN parent_task_id VARCHAR(36)` + DDL 同步 | 现有库执行 ALTER；新库建表后跑批量同步冒烟 |
| A4 | 结算事件日期 str→datetime.combine TypeError（成交触发结算全失败） | `settlement_engine.py:93`、`settlement_tasks.py:429-430` | 引擎入口统一转 datetime，一处转换 | 录一笔成交→触发结算→无 TypeError |
| A5 | 日终对账 100% 失败（account.account_id 不存在） | `reconciliation_tasks.py:92` | 改 `account.id` | 跑日终对账→有输出无异常 |
| A6 | 初始资金双倍入账 | `account_manager.py:91-102` | 入金加幂等键（账户+类型+日期）或先查后插 | 单测：重复初始化只入账一次 |
| A7 | 改密双重加密→下次登录必失败 | `authentication.py:97-101`、`user_repo.py:106-111` | 统一 bcrypt 存储，删 migrated 二次加密分支 | 单测：改密→登出→登录成功 |
| A8 | 登出失效（token="unknown"） | `system_router.py:518-524` | 从 Authorization 头取真实 token 入黑名单；主认证链查黑名单 | 手动：登录→登出→旧 token 再访问被拒 |
| A9 | 路径穿越（file_storage ../ 越权） | `file_storage.py:48-55` | realpath 前缀校验 | 单测：`../` 路径被拒 |
| A10 | SQL 注入 + 硬编码口令（训练服务） | `training_service.py` | 参数化 SQL + 口令移入 .env | 静态检查 + 恶意输入测试 |
| A11 | stock_minutes 无唯一约束（重复数据） | `create_table.sql:2822-2834` | ① 先查重并清理 ② 加 (ts_code, trade_time) 唯一索引 | 查重 SQL 返回 0 后建索引 |
| A12 | 质量检查写硬编码假指标 | `quality_service.py:292-306,357-359` | 真实 SQL 聚合替换假值 | 与手工 SQL 对账 |
| A13 | 告警 metadata/metainfo 键名不符静默丢失 | `monitor_alert_repo.py:72` vs `business_models.py:1585` | 统一列名 | 单测：写入后读回一致 |
| A14 | 告警去重失效（hours=0→created_at>=now 恒不命中→告警风暴） | `alert_service.py:95`、`monitor_alert_repo.py:134-138` | 修复去重语义（hours=0 视为不过滤或默认 1h）+ 默认开启 | 单测：同内容 1 分钟内不重复落库 |
| A15 | 事件 async put 未 await（风险事件永不发布） | `risk_engine.py:358,374`、`risk_manager.py:150` | 补 await | 巡检触发→DB 有 risk_event 记录 |
| A16 | 调用不存在的 event_engine.publish | `integration_service.py:127,205,293` | 改 `await put` | 调用路径不抛 AttributeError |
| A17 | 事件构造缺 enabled 参数必 TypeError | `risk_engine.py:354-361` | 补参数或改事件签名 | 调用 update_rule_params 不抛错 |
| A18 | 两处引用未定义 logger→NameError | `sim_adapter.py:113`、`handlers.py:1027` | 补模块级 logger | 触发路径无 NameError |
| A19 | 引擎工厂循环依赖死锁 | `engine_factory.py:445,512,781` | 依赖解析移出锁 + 先做环检测 | 单测：构造环依赖→报错不死锁 |
| A20 | 事件优先级反转（字符串字母序） | `event_engine.py:38-64` | 排序键改数值映射 | 单测：BACKGROUND 最后处理、CRITICAL 最先 |
| A21 | 队列满时 CRITICAL 被当 NORMAL 丢弃 | `event_engine.py:462-501` | 从 metadata.priority 取优先级 | 单测：塞满队列丢的是最低优先级 |
| A22 | ~~main_engine 三个下划线事件~~ → **移入 D 类 D11**（实证：`enums.py:814`/`engine_events.py:37` 遗留定义，无发布者无消费者，应删除而非"统一"） | `main_engine.py:511-522,762` | 删除死事件类型与死 handler（删除前 grep 订阅关系） | 启动冒烟 + 全库 grep 无残留引用 |
| A23 | 错误码撞值（"5001"/"1006"） | `error_codes.py:105,157,49` | 错误码唯一性启动校验 + 修正撞值 | 启动校验通过 |
| A24 | 回测任务取消失效（executor.cancel 被注释） | `backtest_router.py:258` | 还原为可执行代码 | 手动：发起回测→取消→任务停止 |
| A25 | 被遮蔽死路由（DELETE /batch、GET /feature-sets 永不匹配） | `basket_router.py:261`、`strategy_router.py:987` | 声明移到动态路由之前 | curl 验证两端点可达 |
| A26 | 自选股用错键（user_id vs id→全用户共享） | `market_router.py:340,354` | 改 `current_user.get("id")` | 两账号自选股互不可见 |
| A27 | 告警管理恒 403（读不存在的 can_manage_alerts） | `monitor_router.py:221-329` | 改 permissions 列表判断 | 有权限用户可管理规则 |
| A28 | 交易统计无 user_id 过滤（跨用户泄露） | `trade_router.py:668-718` | SQL 加 `user_id = :uid` | 两账号统计互不可见 |
| A29 | 参数优化 objective 恒 -inf | `optimization_engine.py:199-211` | 注册策略类到优化引擎 | 跑一次优化→产出有限目标值 |
| A30 | 幽灵绩效 -100%（total_assets 恒 0） | `performance_tracker.py:83` | 传入真实总资产 | 策略绩效页不再显示 -100% |
| A31 | 绩效接口恒返回全 0（metrics 键不匹配） | `strategy/handlers.py:584-613` | 读扁平指标键 | 接口返回非零指标 |
| A32 | 主力资金榜用错日期基准恒空 | `dashboard_service.py:101-118` | 独立查 moneyflow MAX(date) | 榜单有数据 |
| A33 | 资金流水按 account_id 查 user_id 列恒空 | `cash_service.py:597` | 维度修正 | 流水列表有数据 |
| A34 | 初始资金/结算记录无幂等键（重复触发重复入账） | `settlement_tasks.py:108-120` | 加 (account, trade_date, type) 唯一约束 + 先查后插 | 同日两次结算只落一条 |
| A35 | 告警渠道 email 未注册但为默认值（只落库不通知） | `alert_manager.py:52`、`alert_engine.py:87-128` | 渠道取交集 + 未注册渠道显式告警 | 告警触发后微信可收到 |
| A36 | strategy_repo 绩效摘要返回写死 0 | `strategy_repo.py:488-509` | 改抛 NotImplementedError 或真实现 | 调用处不再显示假 0 |
| A37 | 🔴 WS 推送 commit 到不存在的 store 模块 → 行情/信号/告警实时推送全失效 | `composables/useWebSocket.ts:83-124` | 统一到 api/websocket.ts 单例 + 重写消息路由 commit 到真实模块 | 前端打开交易工作台→WS 数据实时刷新 |
| A38 | 401 只清 localStorage 不清 store、无 token 刷新、整页跳转丢状态 | `utils/request.ts:104-115` | 同步清 store + refresh token 队列重放 + 路由跳转 | 过期后自动续期不跳登录页 |
| A39 | 假订单提交（OrderForm"提交成功"无 API 调用，幽灵订单） | `components/trade/OrderForm.vue:35-41,164-168` | 接真实 API 或整链删除（该表单当前死链） | 下单入口要么真实要么消失 |
| A40 | SessionScope 对新建 CM 调 __aexit__ 会话泄漏；连接池 dispose 协程未 await 泄漏；死锁重试 rollback 整会话→chunk 静默丢失 | `session_manager.py:146-152`、`connection_pool.py:150`、`repository_base.py:450-451` | 保存原 CM / sync dispose / savepoint 粒度重试 | 单测：会话正确关闭；模拟死锁后前批数据不丢 |
| A41 | token 有效期 30/600/60 三处不一致 | `config.yaml:14`、`config_manager.py:115`、`constants.py:37` | 收敛为单一配置源；基准值**取现状生效值 30 分钟**（行为不变），若要 600 需你确认 | 单测：配置单点生效 |
| A42 | 复权未实现（历史行情接口） | `handlers.py:1024` | 增加复权参数（默认行为不变，返回未复权） | 新参数请求返回复权价 |

> A 类执行纪律：每项独立 commit；先写失败单测（红）→ 修复（绿）→ 全量回归。预计 A 类完成后系统行为只变好、无数字变化。
> **A16 分类实证**（回应"integration_service 是否死代码"质疑）：`grep` 证实 `AnalysisIntegrationService` 位于 `modules/analysis`，被 `analysis_engine.py:47/94/108/119/144/151/158/191` 共 8 处实例化与调用——是**活动模块中的 bug**，不是 messaging 死代码，故保留 A 类修复方向。

## ⚠️ B 类：加防御（低风险——不改主链路业务逻辑）

| # | 问题 | 位置 | 修复方案 | 行为变化说明 |
|:---|:---|:---|:---|:---|
| B1 | 信号确认/取消/待确认/追溯无认证 | `signal_router.py:37-156` | 全部加 `get_current_user` + 交易权限 | 匿名调用被 401 拒绝（合理变化） |
| B2 | WebSocket 无鉴权可订阅订单/持仓/账户频道 | `websocket/routers.py:17-21` | 连接时 JWT 鉴权 + 频道权限过滤 | 未登录 WS 连接被拒 |
| B3 | 500 错误 detail=str(e) 泄露内部信息（30+ 处） | market/account/composite/backtest/system 等 router | 统一"服务器内部错误"，str(e) 仅入日志 | 前端错误提示变通用（需同步前端文案） |
| B4 | 🔴 JWT 密钥公开占位符 | `config_manager.py:114` | 强随机密钥环境注入 + 轮换 | **所有用户强制重新登录一次**（预期行为变化，发布前通知） |
| B5 | 种子 superadmin 明文密码 + DB 故障 fail-open 硬编码超管 | `auth.py:101-145` | bcrypt 随机密码 + DB 故障改 503 fail-closed | 开发环境登录方式变化（文档同步） |
| B6 | 🔴 exec 沙箱保留 __import__ 可 RCE | `strategy_manager.py:183-199` | 移除 __import__ 等受限函数 | ⚠️ **前置审计**：先查 DB `strategies.code` 现有实例是否用到受限函数；若用到先迁移实例代码再收紧 |
| B7 | run-scenario 任意代码执行无沙箱 | `backtest_router.py:664-728` | 限定单用户并发 + 资源限额；长期沙箱化 | 并发受限（可接受） |
| B8 | 登录无速率限制（暴力破解面） | `system_router.py:445-465` | 登录限流 429 + 失败锁定接线 | 连续失败被锁（合理） |
| B9 | 因子定义 PUT/DELETE 无认证 | `data_router.py:1508-1531` | 补 `Depends(get_current_user)` | 匿名修改被拒 |
| B10 | 回测 batch task_ids 无上限（循环查询风暴） | `backtest_router.py:486` | `Body(max_length=50)` | 超长请求被 422 拒绝 |
| B11 | 日终串行同步数小时 | `main_engine.py:230-256` | 改 batch_sync_data 并发 | ⚠️ 并发引入竞态，需验证并发下任务状态机正确（先小并发 2-3） |
| B12 | dashboard 每请求 8 个 DB session | `dashboard_service.py:193-211` | 单 session + 结果缓存 | 无行为变化（响应更快） |
| B13 | 事件队列满丢弃低优先级事件 | `event_engine.py:462-501` | 队列水位监控 + 风险类事件直连告警 | 无 |
| B14 | WS 广播无背压/慢客户端拖垮频道 | `websocket/manager.py:143-160` | 每连接发送队列 + wait_for 超时 | 慢客户端被丢消息（可接受） |
| B15 | 补测试（trade/risk/backtest 8 模块零测试 + 前端 4 文件） | tests/ | 优先补 A 类修复项的单测 | 无 |

## 🔴 C 类：改行为/口径（中高风险——数字会变，逐项回测对账）

> **纪律：每一项独立执行，先"新旧对比报告"再切换；任何一项对账不通过即暂停。**

| # | 问题 | 位置 | 数字变化 | 对账/上线门槛 |
|:---|:---|:---|:---|:---|
| C1 | PIT 停牌前向填充假行情 | `clean_service.py:1267-1316,1403` | **历史行情数据变化** → 因子→信号→回测结果全部漂移 | 修复后全量重算因子 → 与修复前回测对比 → 输出漂移报告 → 确认漂移合理才切换；保留旧数据副本 |
| C2 | 后复权公式错误（÷应为×）+ 前复权锚定漂移 | `adjusted_price_generator.py:126-140` | **所有复权价格重算** → 回测结果变 | 同上：重算 + 新旧回测对比报告 |
| C3 | 清洗规则零匹配空转（激活后清洗真正生效） | `clean_engine.py:1560-1567` | **入库数据开始被清洗** → 下游数据变化 | ⚠️ 先小范围试跑（单日单类型）核对清洗效果正确，再全量 |
| C4 | 绩效口径分裂（夏普三套公式/回撤符号/年化 252 vs 365.25/日盈亏含累计浮盈） | `pnl_calculator.py`、`financial_calculator.py` 等 | **历史绩效数字全部变化** | 统一为 GIPS 口径后：输出新旧绩效对照表 → 用户确认口径 → 前端展示同步加口径标注 |
| C5 | 回测佣金/滑点配置被忽略 | `backtest_engine.py:415-421` | 回测收益变低（更真实） | 新旧回测对比（同样参数）→ 确认差值≈费率影响量级 |
| C6 | 同日两笔卖出超卖（现金虚增） | `backtest_broker.py:1266-1277` | 回测收益下降（虚增被修正） | 新旧回测对比 |
| C7 | T+1 可卖校验缺失 | `backtest_broker.py:313-332` | 回测交易序列变化 | 新旧回测对比交易清单 |
| C8 | NaN 污染账本防护 | `stock_low_high_strategy.py:1207-1211`、`bottom_strategy.py:636,659` | **实盘策略交易序列可能变化**（原本卡死的持仓会正常出场） | ⚠️ 实盘策略代码从 DB 加载——修改后先在回测验证交易序列变化合理，再走"新实例+验证+切换"流程（见详细设计 §5.2 热更新流程） |
| C9 | 回测不可复现（执行用当前代码非快照） | `backtest_service.py:379-394 vs 1283` | 修复后同一任务重跑结果可能不同（用快照） | 明确快照语义；对历史任务标注"旧行为" |
| C10 | 裸信号按 100% 资金买入（weight 默认 None） | `models.py:99`、`sizer.py:209-218` | 无 weight 信号不再满仓 → 回测仓位变化 | 新旧回测对比 |
| C11 | 回测 manager=None UnboundLocalError 等边界 | `backtest_engine.py:505-512` | 异常路径变正常 | 单测覆盖 |
| C12 | 盯市用前复权价与成本混算 | `mark_to_market.py:33-45` | 持仓市值/盈亏数字变化 | 统一复权口径后新旧对账 |
| C13 | 涨跌停 9.8% 近似口径（漏 ST 误计创业板） | `market_state_service.py:51-52` | 市场状态指标变化 → 策略 regime 判定可能变 | 统一 stock_daily_limit 后：重算 market_state_daily → 检查 regime 序列变化 → 确认策略信号不受意外影响 |
| C14 | pnl_outcome 盈亏回填缺失（设计文档声称的"信号→盈亏闭环"核心缺口） | Signal 模型 + `round_trip_service.py`（实时算已实现盈亏但不落库） | 信号表**新增** pnl 字段（不动现有列） | ⚠️ 触及冻结区边缘：只做**最小增量**——新增字段 + 卖出配对时经 SignalRepository.update 回填，**不改动录单 7 步事务本身**。对账：回填值与 round_trip 计算一致 |
| C15 | core 反向依赖 modules（main_engine 直接调 data/account/strategy 服务做日终调度） | `main_engine.py:221-340`（6 处） | 架构重构：回调注册表/事件订阅解耦 | 重构后**日终任务行为必须逐项验证**（市场状态分类/ETF 因子/结算仍按时执行）；建议双轨运行一周对比任务执行日志 |
| C16 | 一表一仓覆盖缺口拆分：① 9 张 SQL 表无 model/repo ② 3 组表名不一致（models vs SQL） | `business_models.py`、`create_table.sql` | ① 先确认 9 表是否在用（可能已废弃）→ 在用则补 repo（A 类新增代码低风险）；② 表名统一**改 __tablename__ 是 C 类**（ORM 映射变化，须逐表对照实际建表名） | ① 补 repo 后 CRUD 冒烟；② 改名后全表 ORM 读写冒烟 |

## 🗑 D 类：死代码清理（表面低风险、暗藏风险——专项引用验证）

> **纪律：删除前每项必须过"三查"：① grep 全库静态引用 ② 动态加载检查（exec/反射/字符串拼接 import/懒加载路由）③ 构建与冒烟。分批删，每批独立 commit。**
>
> **✅ 引用验证已完成（2026-08）**：逐目标三查结果（160+ 目标、含报告过期结论修正与连带 __init__ 清理清单）见 `14-D批死代码引用验证清单.md`。执行删除动作前，对该目标重跑清单中对应的 grep 复核。

| # | 删除对象 | 规模 | 专项风险与验证 |
|:---|:---|:---|:---|
| D1 | utils 15 个零引用文件 | ~2500 行 | ⚠️ `rate_limit` **不删改接入**（B8 用到）；其余 grep 零引用后删 |
| D2 | core/exceptions 异常体系死代码 | ~3000 行 | ⚠️ **先查异常类是否被异常处理器映射表动态引用**（`exception_handlers` 按类名注册）；逐个类 grep 后删，保留处理器引用的 |
| D3 | shared/messaging 全模块（含 message_bus/consumer/producer） | ~1500 行 | ⚠️ 检查 config.yaml/启动流程是否有可选 MQ 开关引用；确认无外部消费者依赖后删。**P0-50 随删除一并解决（不修只删）** |
| D4 | DataManager + Clean/Quality/Research 引擎 + managers/tasks 两包 | ~2000 行 | ⚠️ **主链路"同步后质量检查"是 handlers 直调 DataQualityService——不依赖引擎**；但删除前确认 DataQualityService/DataCleanService 与引擎的代码复用关系（service 被主链路用，引擎是壳）→ 只删引擎壳与 DataManager，service 保留 |
| D5 | 策略文件死分支（bottom _on_bar_trace、GlobalRotationV2 US 分支、stock_low_high 死成员/死参数） | ~300 行 | ⚠️⚠️ **最高风险项**：策略 .py 是模板 seed 源，重启后 seed 到 strategy_templates，DB 实例从模板创建——删分支影响未来新实例。且 C8 类修改也涉及这些文件。**建议：策略文件改动统一走 C 类流程（回测验证），不按纯删除处理** |
| D6 | 前端 8 视图 + ~50 组件 + 6 store 模块 + persistedstate + StrategyLayout + locales | ~35% 前端源码 | ⚠️⚠️ **最高风险删除项，单独放最后执行**：routes.ts 懒加载 `import()` 是动态引用、grep 静态引用会漏。流程：① 先跑 `vue-tsc --noEmit` + `pnpm build` 建基线（含 warning 全量导出）② 按"视图→组件→store→工具"四个小组分 4 批删，**每批删除后 build，逐一核对新增/消失的 warning 与 chunk** ③ DashboardCard.vue 坏 import 说明已有构建隐患，删除批中优先处理。**2026-08 补充（C4 口径标注时实证确认零引用）**：`components/dashboard/PortfolioPerformance.vue`、`components/strategy/MultiStrategyCompare.vue`、`RealTimePerformance.vue`、`PerformanceTable.vue`、`ParameterOptimizer.vue`、`FactorDetailAnalysis.vue`、`components/common/StrategyStatus.vue`、`components/editors/BacktestPanel.vue` 共 8 个组件全库无 import（仅 API 层有同名 TS 类型，非组件引用），并入 D6 清单 |
| D11 | main_engine 死事件（system_started 下划线系 + 3 个无发布者 handler） | 小 | 从 A22 移入：grep 确认无发布者/订阅者后删除 |
| D7 | 前端 api 封装死函数（getStockFactorScores 等）+ 死 composable | ~10 文件 | grep 后删 |
| D8 | 依赖清理（pyqt5/qdarkstyle/qt-material/matplotlib/seaborn/reportlab 待确认） | 依赖项 | ⚠️ 删除前 grep `import PyQt5|matplotlib|seaborn|reportlab`；main.py:8 的 QT_QPA_PLATFORM 引用需同步移除；reportlab 若 PDF 报告在用则保留 |
| D9 | scripts pyc 残留、vite 临时文件、tmp_*.txt、quant_web/dist 提交 | 文件级 | 直接清理 + .gitignore |
| D10 | 被遮蔽路由/未注册 template_router/xtp_adapter（构造即 TypeError） | 小 | 见 A25 修遮蔽路由；template_router 与 strategy_router 合并去重；xtp_adapter 修正或删除 |

---

# 第二部分：执行批次与顺序

## 批次 0：预备（半天）
1. git 检查点（当前分支 dev 打 tag `pre-fix-2026-08`）
2. 测试基线：跑一遍现有 pytest + 前端 build，记录通过率
3. 建立"新旧对比"脚手架（C 类用）：回测结果导出脚本

## 批次 1：A 类紧急 bug（2-3 天，逐项独立 commit）
**顺序**（危险优先 + 依赖优先）：
A1（全表删除）→ A3（缺列）→ A2（Redis）→ A4/A5（结算/对账）→ A6/A7（资金/改密）→ A8（登出）→ A9/A10（路径穿越/SQL 注入）→ A11（唯一索引）→ A12（假指标）→ A14/A35（告警）→ A15-A21/A23（事件引擎）→ A24-A36（其余后端）→ **A37/A38/A39（前端功能 bug）** → A40/A41/A42（会话泄漏/token 统一/复权实现）
**每项验收**：单测绿 + 相关页面手动验证 + 全量回归不新增失败；A37-A39 前端项以 `pnpm build` + 页面手动验证为准

## 批次 2：B 类安全防御（1 天）
B6 前置审计（DB 策略代码受限函数扫描）→ B4（密钥轮换，**提前通知用户重新登录**）→ B1/B2/B9（加认证）→ B3（异常脱敏）→ B5/B7/B8 → B11-B14（性能防御，B11 小并发试跑）→ B15（补测试）
**发布通知**：B4 密钥轮换、B1/B2 认证启用需提前告知用户

## 批次 3：C 类口径（3-5 天，逐项对账）
**顺序**：C4（绩效口径，纯展示层，先行）→ C1/C2/C13（数据重算，跑漂移报告）→ C5/C6/C7/C10/C11（回测正确性，新旧对比）→ C3（清洗激活，小范围试跑）→ C8（策略 NaN 防护，走实例热更新流程）→ C9/C12 → **C14（pnl 回填，最小增量不动 7 步事务）** → C16（表名统一）→ **C17（复权口径活路径统一，2026-08 新增：4 套 hfq 公式仅死链正确，活路径 SQL/前端/验证三处方向错误——先于 D12-5 提取 earliest 查询，方案见 `14-D批死代码引用验证清单.md` §11-9）** → **C15（core 反向依赖重构，双轨运行一周）**——C15 放最后（影响面最大）
**每项门槛**：新旧对比报告经你确认后才算完成

## 批次 4：D 类删除（2-3 天，分组删，前端最后）
**顺序**（低风险先行）：D9（文件清理）→ D1（utils，rate_limit 除外）→ D7（前端 api 死函数）→ D3（messaging）→ D4（数据引擎壳）→ D2（异常体系）→ D11（main_engine 死事件）→ D10 → **D6（前端大清理，单独放最后，4 小组分批 + 每批 build 核对 warning/chunk）** → D8（依赖，grep 后）→ D5 并入 C 类流程
**每批验证**：后端 pytest + 启动冒烟；前端 vue-tsc + build

## 冻结清单（本轮不动）
- **录单主链路**（trade_record_service 7 步事务 + handlers.record_trade + review_signal）
  - ⚠️ 唯一豁免：C14（pnl_outcome 回填）以**最小增量**方式执行——仅新增信号表字段 + 在卖出配对后经 SignalRepository.update 回填，**不改动 7 步事务内任何现有逻辑**；执行前先经你确认
- **自动模式预备项**（原 P0-1/2/4/7/8：ExecutionEngine 落库、sim_adapter、涨跌停模拟校验、execute_signal、双 RiskEngine）——启用全自动前专项修
- 风控阈值数值（校准说明：膨胀期从宽为设计意图，只做 A 类"死常量清理+文档化"，不收紧）

---

# 第三部分：全局回滚与安全网

| 措施 | 说明 |
|:---|:---|
| 独立 commit | 每项修复一个 commit，出问题 `git revert <hash>` 即可回退单项 |
| C 类双轨 | 修复代码合入前，保留旧实现（feature flag 或分支对照），对账通过后删旧 |
| 数据备份 | C1/C2/C13 重算前：`stock_daily`/`stock_adjusted_prices`/`factor_data`/`market_state_daily` 相关区间备份（pg_dump 表级） |
| 回归基线 | 批次 0 记录 pytest/build 基线，每批次结束对比 |
| 冻结保护 | 录单主链路文件（trade_record_service.py 等 5 个文件）在修复期间加 review 备注，禁止顺手改动 |

---

# 第四部分：风险最高的 3 项专项说明

1. **A1 全表删除缺陷**：修一行即可，但**调用点审计必须做**——grep `delete_by_time_range` 全部调用方，确认修复后各调用点的 where 条件语义正确（如"删除 N 天前数据"的保留策略）。修前在测试库验证。
2. **B6 exec 沙箱**：先 `SELECT code FROM strategies` 扫描现有实例代码是否使用 `__import__/eval/exec/open` 等；若有用到，先改造实例代码再收紧沙箱，否则**修复会直接让现有实盘策略加载失败**。
3. **D5 策略死分支删除**：策略文件是模板 seed 源 + 实盘实例从 DB 加载，删除死分支会改变"未来新实例"的代码。全部并入 C 类流程，回测验证后走"新实例+验证+切换"，**不作为死代码直接删**。

---

## 建议启动方式

- 批次 0 + 批次 1 可立即开始（A 类低风险）
- 建议先让我执行 **A1/A3/A2/A4 四个最紧急项**（全表删除、缺列、Redis、结算 TypeError）作为试点，验证流程后再批量推进
- C/D 类每项进入前我会先给出该单项的"新旧对比方案"或"引用验证清单"供你确认
