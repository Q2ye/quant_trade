# 二期优化计划与审计校准

> 2026-09-12 | 前置文档：`10-review报告校准说明.md`（半自动交易 / 资金膨胀期 两前提）、`11-系统级修复实施方案.md`
> 来源：2026-09 五路独立代码审计（回测框架 / 模型训练 / 实盘链路 / 因子挖掘 / 风控监控）+ 开发库 `quant_signals_dev` 只读实测
> **本文档为二期待办清单**。阶段一（基础设施）已完成，见第四部分。
> 2026-09-14 增补第八章（组合与资金分配专项）——该组事项不在 2026-09 五路审计范围内，独立成章。
> **2026-09-25 全量状态核对（第十二章）** —— 57 条逐条回代码复核：**已修 8 · 部分修 9 · 机制已变 2 · 无法核实 3 · 仍存在 35**。
> ⚠️ **引用本文档任一条目前，先看第十二章的状态** —— 正文行号已普遍漂移，且有 7 处表述经核对为失真（§12-6）。

---

## 零、审计结论校准（**先读**——以下结论已被数据推翻，勿再引用）

本次审计中，多个由独立审计 Agent 给出的「致命」结论在回原始代码/查库后被推翻。
按 `10-review报告校准说明.md` 的同一纪律：**涉及改数据或改代码的判断，必须先回原始数据核验**。

| # | 曾被给出的结论 | 实际情况 | 证据 |
|:--|:---|:---|:---|
| 0-1 | 「回测忽略 DB 代码，改用磁盘代码」 | ❌ 不成立。全部策略 `module_path` 为不可导入的 `strategies.user_*`，`importlib` 必失败 → 只走 `exec(DB code)` | 开发库 `strategies.module_path` 全表实测 |
| 0-2 | 「服务端回测被风控杀死，成交近似为 0」 | ❌ 不成立。6 个最近服务端回测 `risk_violations` **全为 0**，成交 143~579 笔 | `backtest_tasks.result` 实测 |
| 0-3 | 「改代码后回测恒跑创建策略那天的旧代码」 | ⚠️ 机制对、后果反。快照机制确实失效，但 `strategy_versions` 表 **0 行** → 回测跑的是**当前** DB 代码；真实含义是**无版本绑定、不可复现** | `strategy_versions` 计数 + `strategy_service.py:797,809` |
| 0-4 | 「僵尸持仓污染持仓判断」 | ❌ 不成立，**是设计**。`PositionRepository` 三个主读取方法均有 `include_zero` 参数且**默认 False**；策略侧恢复另有 `if qty > 0` 双重保护 | `position_repo.py:81,101,117`、`strategy_manager.py:2030` |
| 0-5 | 「最低 5 元佣金未生效（缺陷）」 | ❌ 不成立。**账户为「万一免五」**，`min_commission=0` 才是正确口径 | 用户确认 + `backtest_etf_bottom.py:56` 既有注释 |
| 0-6 | 「`SIMULATED_TRADING` 是安全总开关」 | ⚠️ 与代码不符。生效开关是 `config.yaml` 的 `modules.trade.simulated_trading`；且两分支创建的都是 `SimBrokerAdapter`，**全仓无真实券商适配器**——这与「只能人工交易」的定位一致，非缺陷。<br>**该错误表述曾传播至 9 份文档，已于 2026-09-12 口径统一订正**（`CLAUDE.md`、`AGENTS.md`、`系统目标与投资哲学.md`、`量化交易系统详细设计.md`、`生产环境迁移方案.md`、`信号处理与执行链路设计.md`、`认证与配置设计.md`、`04_回测分析标准流程.md`、`review/00-总报告.md`）<br>附带查实：`生产环境迁移方案.md` 曾声称「production 模式硬编码强制 true」的防护——**该检查不存在**（`main.py` 全部 5 处 `simulated` 引用均为展示/API） | `modules/trade/__init__.py:77-79,96-106`、`execution_engine.py:166-203`、`main.py:248,397,682,1041,1074` |
| 0-7 | 「实盘资金分配错配」 | ✅ 基本自洽。活跃策略 `allocated_capital` 之和 ¥20,555.77 ≈ 账户 `total_balance` ¥20,560.56 | 开发库实测 |

> **另**：审计初稿曾怀疑 `quant_server/scripts/_*.py` 含破坏性 SQL，经大小写敏感复核为**误报**（匹配到注释里的 `truncated`）。全仓脚本无 `DELETE FROM` / `DROP TABLE` / `TRUNCATE`。

---

## 一、可信度与流程（**优先级最高**）

> 这组问题不产生 bug，但让「回测结论」整体失去可信度基础。换策略/选策略前应先解决。
>
> 🔴 **2026-09-25 核对：本组（1-1 ~ 1-4）全部原样未动** —— 即本文档 §十 标为「换策略前必须先有可信的尺子」的第一批，**零进展**。
> 1-5 机制已变（签字 → 复审记录，风险未消）、1-6 已处置、1-7 部分修（有告警无动作、口径≠规范）、1-8 部分修（脚本在、未接门禁）。
> 逐条状态与「证不了什么」见 §12-4 / §12-7 —— **1-8 的分叉现状必须查库才能定论，且它是主池进攻策略恢复运行的前置**。

> **2026-09-15 补充**：准入规范已收敛成文 —— `docs/06-标准规范/06_策略实盘准入标准.md`（六道门 G0–G5 + 三级实盘 + 14 项准入清单 + 签字模板）。
> **2026-09-19 订正**：该文件已修订为 **二级实盘**（S1 小资金实盘 / S2 大资金实盘），清单为 **20 项**。本行所记「三级 / 14 项」为当时快照。
> **规范有了，强制仍然没有**：1-2、1-5、1-6、1-7 描述的全部是「规范与实现之间的差距」，本组优先级不变。

### 1-1 回测无版本绑定，改代码后历史结论不可复现

- **现状**：`strategy_service.py:797` 调 `get_latest_version_number()`、`:809` 调 `unset_current_except()` —— 这两个方法在 `StrategyVersionRepository` 中**不存在**，必然抛 `AttributeError`，被 `:812-814` 吞成 warning。调用点 `backtest_service.py:379-385` 更用 `logger.debug`，生产日志不可见。
- **实测**：`strategy_versions` 表 **0 行** → `strategy_version_id` 恒为 None → 回测走 `strategies.code`（当前代码）。
- **影响**：改一次代码重跑，历史回测结果无法复现；`backtest_service.py:1490` 那段「C9 修复：优先用版本快照…」的注释描述的修复**从未生效**（死代码）。
- **建议**：① 实现缺失的两个方法（或改用 `get_current_version` 语义）；② 或在每次编辑代码时显式建快照；③ 快照创建失败必须 ERROR 级告警，不得静默。

### 1-2 上线门槛零强制

- **文档标准**（`docs/00-核心策略体系/牛市择时追强研发立项.md:70-74`）：年化中位 ≥30% / 下四分位 ≥15% / MDD <30% / 夏普 >1.0。
- **代码实现**：**零**。全仓无任何校验策略准入收益/回撤/夏普的代码。
- **唯一关卡**：`execution_service.py:170-177` 一句 `logger.warning`，且 `strategy.status` 是用户在前端下拉手选的（`StrategyWorkspace.vue:22-27`）。
- **规范已补齐（2026-09-15）**：准入规则已收敛为 `docs/06-标准规范/06_策略实盘准入标准.md`（六道门 G0–G5 + ~~三级实盘~~ **→ 2026-09-19 修订为二级实盘 S1 小资金 / S2 大资金** + **分层 20 项准入清单**：〇层口径申报 / ①层通用底线 4 条 / ②层定位线按类型 / ③层门槛项 10 项）。准入线**按策略类型分层**（进攻型卡年化中位、防守型卡跑赢国债+降回撤、卫星赔率型卡盈亏比），不搞一刀切。**差距仍在本条 —— 规范有、强制无。**
- **建议**：按 `06_策略实盘准入标准.md` §四 的 20 项做启动前校验（**①层 4 条底线可机检，优先实现**），不达标则拒绝 `run_mode=live` 启动（或强制二次确认）。
- ⚠️ **前提依赖**：**1-1 是本条的前提** —— 准入判断的对象是「某次回测结果」，无版本绑定则「达标」不可复现。

### 1-3 参数优化器不可用

- **共享策略对象**：`optimization_engine.py:199` 取 `_strategy_instances[strategy_id]`，`:222-223` 每个 trial 改同一对象，`GridSearch`（`grid_search.py:56-59`）用 `asyncio.gather` 并发 → A 的 `on_bar` 读到 B 的参数。
- **不按 ID 取类**：`:208-214` 取注册表里**第一个有类的类型**，与传入的 `strategy_id` 无关 → 可能优化的不是目标策略。
- **口径不一致**：目标函数 `sharpe + annual_return*0.5`（`:315`）用 365 自然日年化，与报告口径（252 交易日）不同源。
- **好消息**：优化结果**不落库、不进前端排行**（`backtest_tasks` 无 optimization 列）。
- **建议**：修 `strategy_id` 映射 + 每 trial 独立策略实例；或直接禁用该 API 直至重写。

### 1-4 脚本回测与服务端回测不同源

| 维度 | 服务端 `run_backtest` | `scripts/backtest_*.py` |
|:---|:---|:---|
| 策略代码 | DB `strategies.code` | 磁盘 import |
| 风控 | 调实盘风控引擎（规则未启用→实际不拦） | 零调用 |
| 成本 | 佣金+印花税+过户费+滑点 | **已于阶段一补齐**（`high_vol`/`small_cap`；`cross_market` 原有；`etf_bottom` 佣金口径正确） |
| 涨跌停 | 有（open 模式） | 无 |

- **建议**：明确「以哪条路径为准」，并在文档中固化；两边结论不可互换引用。

### 1-5 准出决策无责任人（无裁决签字 / 复审无否决权）

- **现状**：六份规范文件的产出**全是报告 / 清单 / 模板**，无一处定义「谁拍板上线、谁担责」—— 依次为 `strategy-auditor`、`strategy-review`、`rules/strategy-gates.md`、`rules/audit-strategy.md`、`strategy-dev`、`量化交易系统详细设计.md` 第五部分。
- **已有的第二道防线**：`.claude/agents/strategy-reviewer.md`（只读复审 agent，**作者不自审**）—— 但**结论无否决权、不进任何闸门**。
- **影响**：准入判断实质是「作者自证」。与业界「独立验证 + 投委会签字」（银行 SR 11-7 三道防线范式）差距最大的一环。
- **建议**：复审结论 + 本人签字一并记入《准入决议》（模板见 `06_策略实盘准入标准.md` §四），**无签字 = 无准入**。单人项目按「非作者独立复审一次」降维实现即可，不求组织化。
- ⚠️ **2026-09-25 核对：机制已变，风险未消** —— `06_策略实盘准入标准.md:111-119` 已于 2026-09-21 把「裁决人签字」改为「**独立复审记录**」，**明文不再要求人工签字**；`strategy-reviewer` 仍无否决权、不进任何闸门；`docs/` 下无任何 `*准入决议*` 文件。
  → **作者自证的结论未变，只是不再叫「签字」。** 本条不应按「已处置」关闭。

### 1-6 无上线前纸上交易期（S1 缺失）

> ✅ **2026-09-19 已处置（本条关闭，勿再按缺口执行）**：`06_策略实盘准入标准.md` §三 **三级实盘 → 二级实盘** ——
> 原 **S1 纸上交易**（`draft`、不绑账户）与 **S2 小资金实盘** **合并为「S1 小资金实盘」**（绑账户、**≥1 个月**），原 **S3 全量** 改称 **「S2 大资金实盘」**。
> **paper 阶段有意取消**（无真实成交 → 验不了滑点/成交对账，而这三项恰是本项目最缺的）；风险改由**资金上限（≤目标仓位 20–30%）**与 **§五 强制降级阈值**控制。
> 升级判据改为**五条可立即执行**的判据（§3.1），原「2σ 偏差」降为补充判据（监控代码零实现）。
> 详见 `06_策略实盘准入标准.md` §三 / §3.1 / §附。**以下原始分析保留作历史记录。**

- **现状**：「观察 1-2 周」（`量化交易系统详细设计.md` 第五部分 热更新五步）是**上线后**新旧实例并行观察，**不是上线前的模拟盘 gate** —— 没有定义上线前必须先跑一段无资金/小资金的强制期。
- **已有的替代物**：实盘验证清单 V1–V11（人工造场景预演）—— 能验状态机缺陷（F1–F9 / B1 / B4），**验不了真实滑点、数据延迟、成交对账**。
- **建议（原）**：强制 S1 阶段（`run_mode=draft` / 不绑账户）**≥ 20 个交易日或 ≥ 10 笔信号**，再进 S2 小资金实盘（≤ 目标仓位 20–30%，≥ 1 个月）。分级与升降级条件见 `06_策略实盘准入标准.md` §三。

### 1-7 失效降级只有问句，无阈值无动作

- **现状**：热更新规范写了「退市风险过滤 + 实盘失效监控（**何时该停用**）」，但**无阈值、无自动动作**；`strategy-review` C3 亦只设问不给数。运行期唯一告警是「实盘 vs 回测偏差每周对比 > 2σ」（需人工响应）。
- **影响**：失效识别依赖人工偶然发现 —— 而本项目「回撤大」不构成可执行的停用信号（跨市场 7.6 年口径 84.5% 的交易日处在某段回撤中）。
- **建议**：给出可执行阈值并接入 `monitor` 告警。`06_策略实盘准入标准.md` §五 已拟 4 条：回撤 ≥ 设计 MDD×1.2 → 降级 / 连续 3 月跑输基准 → 暂停复核 / 偏差连续 2 周 >2σ → 暂停归因 / 信号频率偏离 >50% → 暂停（**先查数据链路，不查策略逻辑**）。数值需实盘校准，先按建议值落地再调。

---

### 1-8 live 实例的 DB `code` 可能长期落后于磁盘（实测 3/7 分叉）🔴

- **2026-09-15 实测**（策略从 DB `strategies.code` 加载，改磁盘**不影响**实盘/回测）：

  | 实例 | 状态 | 与磁盘 |
  |:---|:---|:---|
  | 跨市场动量避险轮动-实盘-2.0 | **running** | ✅ 一致（**唯一在跑，安全**） |
  | 高波动动量轮动策略-**7.1**-实盘 | paused | ⚠️ **分叉**（**主池进攻，恢复运行前必须处理**） |
  | 跨市场 v1.0-实盘 | paused | ⚠️ 分叉（旧版本，已被 2.0 取代，预期内） |
  | 进攻实盘-1（低吸） | stopped | ⚠️ 分叉（参考策略） |
  | 熊市防守-01-实盘 / 防守实盘-1 / LightGBMBottom-实盘-防御型 | paused/stopped | ✅ 一致 |

- **实例证据（ETF 底部 3 副本）**：整改凭证时发现其 DB 副本停留在 **2026-08-05** 的版本 ——
  `capital = float(getattr(self.context, "available_capital", 0) or 0)`，
  而磁盘在 **2026-09-06**（提交 `0285104`）已改为契约要求的 `self.resolve_available_cash()`。
  → **那次 sizing 修复从未同步到 DB，3 个 live 实例（当时均 stopped/paused）一直跑着违反
  `docs/01-业务设计/策略资金与仓位契约.md`「策略永远只通过统一访问器读资金」的旧代码。**
  已在本次凭证整改中一并同步（4 副本已对齐磁盘）。
- **风险**：分叉静默存在 —— 无任何机制比对磁盘与 DB 版本；一个 paused 实例被恢复运行时，
  跑的是**几个月前**的代码，而开发者以为刚改完已生效。
- **建议**：① 新增「磁盘↔DB code 一致性巡检」脚本（比对哈希，纳入日终或提交前检查）；
  ② 上线/恢复运行前**强制比对**（并入 `06_策略实盘准入标准.md` G1 代码门）；
  ③ 高波动 7.1 的分叉需先 diff 确认是「遗留旧版」还是「独立线上补丁」再决定同步方向。

## 二、因子体系

> 🔴 **2026-09-25 核对：本章唯一已修的是 2-6**（DB 元数据路径，✅ 见该条内注）。
> 2-1 / 2-2 / 2-3 / 2-4 / 2-5 / 2-8 **全部原样未动**（含 2-8 的 IC 写回仍是**死路径**：`factor_research_repo.py:272-317` 全仓无调用方）。
> 2-7 **部分修**（命名规范已成文，补注册与大小写清理未做）。
> ⚠️ 本章行号整体后移约 8 行；2-5 的两处**文件路径在文档里写错**（已就地订正）。逐条状态见 §12-4。

### 2-1 基本面因子存在未来函数（最多约 4 个月前视）🔴

- `research_service.py:2344` 取 `stmt.end_date`（报告期）作索引键，`:2457-2462` 按日历日 `ffill`。
- 后果：2024 年查询区间内，FY2023 年报（`f_ann_date≈2024-04-25` 才公告）从 **2024-01-01** 起即被填入 → PE/PB/PS/ROE/ROA/GM/OM/DR 等全部受影响。
- `:2314` 的 `f_ann_date <= query_date` 过滤**挡不住**（`query_date` 取区间末）。
- **被掩盖**：`tests/modules/test_pit_financial.py` 测的是本地 mock 副本，恰好没覆盖「按 end_date ffill」这一步。

### 2-2 分位数/稳定性分析用「当日收益」冒充「前瞻收益」

- `research_service.py:1623-1624`（分组）、`:1850-1851`（稳定性）取 `returns_data.loc[dt]`＝当日收益；
  而同一份报告的 IC 分析用 `dt_idx + forward_period`（`:1496-1498`）。
- **后果**：同一报告两套口径 → 动量类因子的多空收益、单调性、`top_minus_bottom` 被系统性夸大。

### 2-3 横截面标准化混装 ETF 与股票（潜伏）

- `research_service.py:1355-1358` 取「当日该因子全量行」，而 `factor_data` 同时含 ETF 与股票。
  - ⚠️ **2026-09-15 订正**：本节原写「1682 只 ETF + 319 只股票」**与实测不符** —— 实测 distinct `ts_code` = **2,001**，且 **`factor_data` 表根本没有 `asset_type` 列**（`create_table.sql:3445-3458`）→ **分层标准化的落地前提本身缺失**（无法在 SQL 层区分 ETF/股票）。
- 目前 `z_score` / `percentile` / `rank` 实测**非空行数均为 0**（主会话复核）、无策略读取 → **未爆**；一旦启用标准化列即爆发。

### 2-4 标的池用全样本统计量筛选 + 股票因子用不复权价

- `scripts/data/backfill_top100_factors.py:366-370` 用 2024 起全样本波动率选池，`:394` 再回填 2020 起数据 → 前视/幸存者偏差。
- `research_service.py:1063-1086` 取 `stock_daily` 原始 close → 除权除息日伪跳空，动量/波动因子失真。

### 2-6 `get_factor_metadata` 的 DB 路径**恒失效**，永远走硬编码兜底 🟠 ✅**已修（2026-09-25 核对）**

> ✅ **2026-09-25 核对：本条已修**（commit `a005a14`，2026-09-15）。
> `research_service.py:891` 已改用真实字段 `factor.factor_name`；`:897` `update_frequency` → `factor.calculation_frequency`；
> `:895-896` 的 `data_source` 键已删；`:905-913` 由宽 `except Exception` **收窄为 `except (AttributeError, TypeError)` 并 `raise`** → 属性错误不再被静默吞掉。
> 模型侧 `data_models.py:910-935` 确认那三个属性仍不存在（用的是上面括号里那批真实字段）→ **映射已对上，DB 路径通了**。
> ⚠️ 「能真正读到数据」仍需一次带 DB 的调用验证（`is_active=True` 有无行）—— 本次核对未连库。
> **以下原始分析保留作历史记录。**

- **代码路径**：`research_service.py:893-898` 读取 `factor.display_name` / `factor.data_source` / `factor.update_frequency` —— 而 `FactorDefinition` 模型（`data_models.py:910-931`）**这三个属性一个都没有**（实际字段为 `factor_code` / `factor_name` / `factor_type` / `category` / `description` / `formula` / `parameters` / `data_requirements` / `output_type` / `calculation_frequency` / `is_public` / `is_active` …）。
- **后果**：必然抛 `AttributeError` → 被 `:906-908` 的 `except Exception` 兜住（仅记 ERROR）→ **恒走 `_get_standard_factor_metadata`（`:3049`）硬编码兜底**。**DB 里的因子定义永远读不到** —— 与 `strategy-gates.md` 要求的「新增因子必须注册到 `factor_definitions`」形成闭环断裂：**注册了也读不到**。
- **影响面**：任何依赖因子元数据的下游（前端因子列表、策略因子校验）拿到的都是硬编码清单。
- **建议**：修属性名映射（`data_source` / `update_frequency` 若语义需要，应在模型补字段）；把 `except Exception` 收窄，**不允许属性错误被静默兜底**（否则缺陷永远不可见）。
- **来源**：`docs/06-标准规范/03_因子研究标准流程.md` §6.2 复核发现（2026-09-15）；本行已由主会话二次验证。

### 2-7 因子注册缺口：28 个因子**有数据未注册**，含 8 组大小写变体 🟠

- **实测**（开发库只读，2026-09-15，主会话二次复核一致）：`factor_data` **48,054,818 行 / 102 个** distinct `factor_code`，而 `factor_definitions` 仅 **83 行** → **28 个因子有数据但未注册**。
- 其中 **8 组是同一因子的大小写变体**：`macd`/`MACD`、`pb`/`PB`、`ret_1m`/`RET_1M`、`ret_3m`、`ret_6m`、`vol_1m`、`vol_3m`、`boll`/`BOLL`。
- **影响**：`strategy-gates.md`/`strategy-dev` 的「新增因子必须注册到 `factor_definitions`」在现状下**无法自洽**（注册表本就不全，且 2-6 使其"注册了也读不到"）；大小写变体会让同一因子被当成两个。
- **建议**：补注册 + 统一命名规范（见 `docs/06-标准规范/03_因子研究标准流程.md` §1.3）。

### 2-8 因子 IC 证据**从未落库** 🟠

- **实测**：`factor_research` **34 行全部为 `ic_analysis` + `completed`**，但 `ic_mean` / `top_minus_bottom` **非空行数均为 0**。
- **影响**：任务标记"完成"而 IC 字段全空 → 「新因子必须给出 IC / 分组回测证据」这条要求**在库里查不到任何支撑**，等于因子有效性从未被记录（连带 `factor_data` 的 102 个因子中，绝大多数无有效性证据）。
- **建议**：修 IC 写回链路；把「IC 字段非空」作为任务 `completed` 的判据。

### 2-5 能力缺口

- **无**行业/市值中性化、因子正交化（`:1767` 仅算相关系数均值）、因子级合成。
- `factor_definition_repo.py:238` `_dict_to_json` 函数体为 `pass`（潜伏 bug）。
  > ⚠️ 2026-09-25 核对：**路径应为** `shared/database/repositories/analysis/factor/factor_definition_repo.py`（在 `analysis/factor/` 子目录下，非仓库根）；**缺陷本身仍存在**（`:238-239` 仍为 `pass`）。
- `factor_ic_test.py` 392 行但**无任何 `def test_`**，pytest 永不收集（是研究脚本，非测试）。
  > ⚠️ 2026-09-25 核对：**路径应为** `tests/modules/test_strategy/factor_ic_test.py`；`grep -c "def test_"` 实测仍为 **0**，仍存在。
- 因子体系目前**只服务 ETF 底部一个策略**；高波动/跨市场两条主力策略零 `factor_data` 引用。

---

## 三、模型训练

> 🔴 **2026-09-25 核对：本章 3-1 / 3-2 / 3-3 全部原样未动**（`training_service.py` 自 2026-08-22 `0bbde2e` 后未再改动，行号与文档逐字吻合）。
> 仅 **3-1 的一处表述需订正**（`model_trainings` 的写入路径**存在**，见该条内注）；**3-4③ 无法核实**（需查库，且其因果链与文档相反 —— DB 参数在**实盘不生效、只在回测生效**）。
> 逐条状态见 §12-4。

### 3-1 线上模型与验证对象不是同一个 🔴

| 版本 | 特征 | 阈值 | 用途 |
|:---|:---|:---|:---|
| v1 / v1_backup | 42 | 0.55 / 0.60 | **回测与调参实际使用**（`backtest_etf_bottom.py:61,64`、`sweep_params.py:60` 只 glob `etf_bottom_v1_*`） |
| v2 | 44 | 0.54 | `training_service.py` 产出（文件名硬编码 `v2_`） |
| v3 | 44 | 0.52 | 无来路 |
| **v5** | **53** | **0.48** | **线上实际加载** |

- 线上加载方式是 `bottom_strategy.py:161-175` 的 `sorted(glob("etf_bottom_v*.joblib"), reverse=True)[0]` —— **文件名排序加冕**，无审批/无注册/无回滚。
- v3/v5 **无法从仓库复现**（训练区间不符、缺 `label_type`/`n_train`、全 git 历史无引用）。
- `model_trainings` 表 **0 行** → 训练 API 从未成功执行。
  > ⚠️ **2026-09-25 核对订正**：写入路径**存在** —— INSERT 在 `api/routers/strategy_router.py:966-1008`（`:971` 建行、`:987` commit），
  > UPDATE 在 `training_service.py:340-352`。故「API 从未成功执行」**不能由代码层断定，需查库计数**。
  > 另核实：`training_service.py:286` 硬编码 `etf_bottom_v2_{today}.joblib` → 训练**永远产不出 v3/v5**；
  > `grep -a label_type` 实测 v2/v3=1、**v5=0**，`n_train` v1/v2/v3=1、**v5=0**（v5 不是当前流水线的产物）。

### 3-2 训练侧前视与口径不一致

- `training_service.py:227` `ffill().bfill()` 在 **train/val/test 切分之前**对全量双向填充 → `bfill` 用未来值回填缺失。
- `:228` 训练用 `X.fillna(X.median())`（中位数含 val/test）；线上 `bottom_strategy.py:404` 用 **0 填充**。
- `:236` 标签窗口（T+1..T+10）跨越切分边界。

### 3-3 标签失真

- `training_service.py:221` 用 `target >= 1` 合并为二分 → **最大回撤条件 Y 被完全丢弃**。
- 开发库实测正样本率 30%~49.5% → 该标签实际识别的是「波动率」而非「底部」。

### 3-4 工程缺口

- 无定期重训、无模型注册表、无衰减监控（仅有静态 `auc_val/auc_test`）。
- `lightgbm` / `scikit-learn` / `joblib` **不在 `pyproject.toml`** 依赖清单中。
- 代码中「剔除 159 创业板 9 只 / 候选上限降 2」的安全收紧，被 DB `strategy_parameters`（建于更早）架空 → **从未生效**。

---

## 四、阶段一已完成（备查）

| 项 | 结论 | 提交 |
|:---|:---|:---|
| 确认/录单幂等 | ✅ 已实现并验证。`confirm`/`cancel` 加前置状态守卫（409）；`record` 加**累计数量**护栏（非「一信号一单」——那会误拦分笔成交）；`update_signal_status` 修恒返回 True | 未提交 |
| 僵尸持仓 | ⛔ **取消**。经核验是设计（`include_zero`），非缺陷；**未删除任何持仓数据** | — |
| 回测脚本交易成本 | ✅ `backtest_high_vol_momentum.py` / `backtest_small_cap.py` 补成本（免五口径：佣金万1 无门槛 + 印花税 0.1% 卖出 + 过户费 0.002% + 滑点 0.02%）；`backtest_etf_bottom.py` 经核实无需改 | 部分提交 |
| 滚动起始日下四分位 | ✅ `rolling_start_analysis.py` 补 `P25` | `6092867` |
| 脚本治理 | ✅ 33 个一次性诊断脚本入历史后移除（内容存 `450ccf2`）；10 个长期资产补入 git；2 个过拟合扫参脚本标注废弃 | `6092867` / `450ccf2` / `ca53f3a` |

**成本量化**：免五口径下往返费率恒为 **0.124% + 滑点 0.04% ≈ 0.164%**（与金额无关）。此前 `high_vol`/`small_cap` 脚本零成本，收益被系统性高估。

---

## 五、监控与告警

| # | 问题 | 证据 | 影响 |
|:--|:---|:---|:---|
| 5-1 | **风险类告警全部落入黑洞** 🔴 | 发布端 `risk_events.py:118` 为 `risk.alert.triggered`，订阅端 `alert_engine.py:75` 为 `monitor.risk.alert.triggered` —— **字符串不匹配**，且全仓无该事件订阅者 | 阈值突破/信号违规/严重突破**永不推送**；前端「风险告警」面板恒为空 |
| 5-2 | 风控事件零落库 | `risk_engine.py:613-616` 的守卫要求 `rule_id`/`user_id`，而 `check_signal` 构造的 violation**从不含这两个字段** | `risk_events` 表恒空，违规无审计留痕、重启即失忆 |
| 5-3 | 微信通道把失败当成功 | `wechat_alerter.py:65-71` 只判 HTTP 200，**不解析企业微信返回体 `errcode`** | webhook 失效时仍记为「已送达」，静默丢消息 |
| 5-4 | 邮件通道未实现 | `EmailAlerter` 类**在代码中不存在**；`alert_engine.py:87-128` 无 email 分支 | 文档称「已实现未注册」与代码不符 |
| 5-5 | 风控双实例脑裂 | `modules/risk/__init__.py:100` 与 `modules/trade/__init__.py:161` 各注册一个 `risk_engine`；前端操作后者、`SignalEngine` 持有前者 | **前端关闭规则对实盘无效** |
| 5-6 | `expire_stale_signals` 定义但从未调用 | `signal_repo_v2.py:40`（全仓无调用方） | `pending_manual` 永不自动过期 |
| 5-7 | **数据完整性门只查 `stock_daily`，ETC/指数不查** 🟠 | `core/engines/system/main_engine.py:252-284` `_data_integrity_gate`（调用点 `:235-243`）；日终 4 个 pre_gate 任务（`modules/data/__init__.py:217-296`）**全部非致命** | 「`stock_daily` 齐全但 ETF/指数数据缺失」时**策略仍被驱动** → ETF 类策略用陈旧价算信号（跨市场策略已用 F9 守卫在策略层兜底，但**框架层无门**） |
| 5-8 | **质检极值判据对 A 股不成立** 🟠 | `quality_service.py:594-605` 用统一 `\|close-open\|/open <= 0.2`（2026-09-25 实测已移至 `:789-790`） | A 股主板 ±10% / 双创 ±20% / ST ±5% —— 统一阈值会**误删合法数据**（双创股 20% 波动合法却可能被判异常）。应按 `stock_daily_limit.up_limit/down_limit` 逐股判定 |
| 5-9 | 质检配置**死配置** | `_load_quality_config`（阈值 95.0/90.0）**无任何读取方** | 配置改了不生效，且无人察觉 |
| 5-10 | `amount` **跨表三种单位**（规范只写了千元）🟠 | DDL `COMMENT` 实测：**千元** = `stock_daily`/`index_weekly`/`index_factor_pro_daily`；**万元** = `etf_daily`/`etf_minute`/`index_daily`/`stock_moneyflow.*`；**元** = `stock_minutes`/`stock_weekly`/`stock_monthly` | 跨表拼接产生 10×/1000× 错配。**`04_回测分析标准流程.md` §2.3 已于 2026-09-15 订正**，但代码中**无任何单位换算守卫** |
| 5-11 | 复权因子一致性检查**只有规则无实现** | `.claude/rules/audit-backend.md` 要求「相对误差 < 1e-6」，全仓无实现代码 | 复权错配无人守 |
| 5-12 | **事件优先级被解读反** 🔴（2026-09-15 已修） | 两套方向相反的优先级枚举并存：`core/events/types.py:15` `EventPriority`（IntEnum，LOW=10…CRITICAL=100，**越大越紧急**，**`BaseEvent` 实际用的就是它**）vs `core/engines/types/enums.py:206` `PriorityLevel`（str 枚举，映射 1=CRITICAL…5，**越小越紧急**）。`event_engine.py` 的归一化 `_to_value` 只转换 `PriorityLevel`/`str`，而 `EventPriority` 是 IntEnum → 命中 `isinstance(p, int)` → **原值透传** | 堆按 10..100 排序 → **LOW(10) 最先出队、CRITICAL(100) 最后出队**；连带 `put` 的队列溢出保护判据 `priority <= 2` 对任何 `EventPriority` 都不成立 → A21「CRITICAL 不被丢弃」的修复**从未生效**。**修复**：新增模块级 `_normalize_priority()` 统一尺度，`_to_value` 与 `put` 均改用之 |
| 5-13 | **类式订阅静默失效** 🟠（2026-09-15 已修） | `BaseEvent.event_type` 是 **property**（`core/events/base.py:141`），对**类**做 `getattr(cls,'event_type')` 得到的是 property 对象而非字符串；`EventEngine.subscribe` 旧实现把它 `str()` 成 `'<property object at 0x...>'` 当作订阅键 | 事件能入队、统计显示"已处理"（`processed_events` 增长），但**永远匹配不到处理器，且无任何报错**。生产代码因事件类恰好声明了**类级字符串** `event_type` 而幸免；任何在 `__init__` 里设 `event_type` 的类都会中招。**修复**：`subscribe` 只接受真字符串，否则回退 `EVENT_TYPE` → 类名；`_process_event` 增加类名回退查找 |

> ⚠️ **2026-09-25 核对：本章两条订正（正文表格内无法就地加注，故记于此）**
>
> **（一）5-10 的「09-15 订正」本身是错的** —— 上表把 `etf_daily` / `index_daily` 归为「**万元**」，但 DDL 实测两张都是 **千元**
> （`etf_daily:3243` / `index_daily:3319`）。`04_回测分析标准流程.md:69` 已于 **2026-09-19** 明写「09-15 那次订正把这两张改错了」
> → **两份标准文档互相矛盾，且后者指向前者错了**。**引用单位口径时以 `04_回测分析标准流程.md` 为准**。
> 另：本条「代码中**无任何**单位换算守卫」也已过时 —— 已有显式换算：`high_vol_momentum_strategy.py:544`(×1000)、
> `shared/.../etf_daily_repo.py:487`(×1000)、`etf_minute_repo.py:465`(×1000)；ORM `data_models.py:841` 注释仍写「万元」与 DDL 冲突。
>
> **（二）5-5 的方向写反了** —— 文档写「前端操作 **trade 侧**、`SignalEngine` 持 **risk 侧**」，实测按 `main.py:764 _sort_modules_by_dependency`
> 的拓扑序（risk 在 trade 之后）**risk 侧后写覆盖** `_module_engines["risk_engine"]` → **前端开关打到 risk 侧**、
> `SignalEngine`（`trade/__init__.py:150-156`）持 **trade 侧**。**结论（前端关规则对实盘无效）仍成立，但因果链相反**。
> 附带：两端**都订阅** `risk.check.requested`（`risk_engine.py:102`）→ 同一次检查跑两遍。
>
> **逐条状态**（明细见 §12-4）：5-1/5-2/5-3/5-4/5-5/5-6/5-8/5-10 **仍存在**；5-7 部分修（门已扩 ETF/指数 + 多一层可阻断质检门）；
> 5-9 部分修（有赋值但无人消费）；5-11 部分修（实现落在**未入库**的 `scripts/_diag/`）；5-12 / 5-13 **已修** ✅。

> **说明**：风控规则本身**只启用 1 条**（`limit_up_down`）是**定位选择**（资金膨胀期，从宽），非缺陷。
> 主策略的资金保护在**策略层**：`high_vol_momentum_strategy.py` 自带 ATR 硬止损（2.0/2.5×ATR）+ 移动止损（2.0/3.5×ATR），`_check_stops_and_trailing()`（`:746`），**不依赖风控引擎**。

---

## 六、安全

### 6-1 硬编码数据库口令 🔴

13+ 处 `password: "123456"` 明文，含**生产服务与线上策略本体**：

```
modules/data/services/market_state_classifier.py:40          ← 生产服务
modules/strategy/strategies/etf/bottom_strategy.py:77,206,297,319  ← 线上 ETF 底部策略
modules/strategy/strategies/etf/bottom_strategy.py:206/297/319 为 self.parameters.get("db_password", "123456")——口令作为运行时兜底
scripts/ 下 9 个脚本
```

**为什么 hook 没拦住**：`CLAUDE.md` 描述的 PostToolUse 凭证扫描**只对新写入的文件生效**，扫不到既有文件。

> ⚠️ **2026-09-15 订正 —— 上述根因解释不完整，实测是两条叠加**（详见 §九 9-1~9-4）：
> ① matcher 只挂 `Write` → **用 `Edit` 往已有文件加凭证根本不触发**（**9-1**）；
> ② **`sys.stdin` 在 Windows 按 GBK 解码**，含中文的 payload 解析失败 → hook 静默返回 0（**9-3**）。
> 两条已全部修复；并 **新增 `PreToolUse` 版凭证拦截**（`hooks/scan_incoming.py`，写之前拦、检查即将写入的内容），
> **端到端实测可拦下含中文的写入**。
>
> ⚠️ 另注：本条曾把「hook 静默」的第三条根因写成「`PostToolUse` 的 `exit 2` 不回喂模型」——
> 经修好编码后复测，该说法**是误诊**（见 **9-4**）：真因仍是 ②，hook 根本没走到 exit 2。
> `PostToolUse` 的 `exit 2` **会正常回喂**。

> ✅ **已修部分（2026-09-15）**：`etf/bottom_strategy.py` 的 4 处已清除 —— 改为模块级
> `_pg_conn_cfg()` 从 `shared.config.settings.DATABASE` 读取（与连接池同源），
> **4 个 DB 副本已全部同步**（3 实例 + 1 template）。
>
> ⚠️ **剩余量实测（2026-09-15）**：全仓仍有 **20 个文件**含 `123456` ——
> `modules/data/services/market_state_classifier.py`（§6-1 已列）、
> **`modules/market/services/limit_service.py`（§6-1 漏列的另一处生产服务）**、`scripts/` 下 18 个。
>
> ⚠️ **但真正的问题不是代码**：该口令是**开发库**的（`.env` 的 `DEV_DATABASE__PASSWORD` 本身即 `123456`）。
> 代码里的 `123456` 只是"兜底默认值"，**删掉默认值不会变安全** —— 需要先把**弱口令本身**换掉。
> **这一条需要人工决定（`.env` 禁止由 agent 写入）**。

> ⚠️ **2026-09-25 核对订正（本条有两处失真）**：
> ① **`modules/market/services/limit_service.py` 是假阳性** —— 该文件 `grep -i password` **零命中**；
> 命中的是 `:219` 的 `"seal_amount": 1234567.8`（`.8` 被 `123456` 子串匹配）；`git log -p --all` 该文件历史中**从未出现 password**。
> ② **计数已过时**：现为 **50 个文件**（`scripts/_diag/` 35 + `scripts/data/` 9 + `scripts/ops/` 4 + `scripts/backtest/backtest_etf_bottom.py` 1 +
> `modules/data/services/market_state_classifier.py:40` 1），另加文档未列的 `shared/config/config_manager.py:60` `DatabaseSettings.PASSWORD: str = "123456"` 兜底默认值。
> ✅ `bottom_strategy.py` 的 4 处**确已清除**（`:28-47` `_pg_conn_cfg()` → `:227,312,328` `psycopg2.connect(**cfg)`，该文件 `123456` 0 命中）。
> ⚠️ 口令本身仍是开发库弱口令 —— 「删代码默认值不改变安全性」这一判断不变。

> 按用户决定，本项**留二期**。

---

## 七、实盘运行观察（需业务侧确认）

| # | 现象 | 数据 | 需确认 |
|:--|:---|:---|:---|
| 7-1 | 全部 `run_mode='live'` 策略状态均非 running（2 paused / 3 stopped），**但仍在逐日出信号** | 近 90 天 86 条信号，最近 2026-09-12 | `paused` 的语义是否为「不拦截信号生成」？ |
| 7-2 | 70% 信号被拒（60/86） | `高波动 7.1 实盘` 29 条、`熊市防守` 27 条 | 其中量能确认/收盘确认属设计；但下述 7-3 是异常 |
| 7-3 | **同一标的信号价被冻结、逐日重复拒绝** | `513050.SH` px=1.1140 连续 8 个交易日被拒（市价 1.03~1.08）；`512100.SH` px=3.0349 共 10 次 | 信号价恒定说明取到**陈旧值**（`bottom_strategy.py:811` 的 `signal_low`，来自 `_restore_confirm_buffer` 的 `bar_map`）→ 确认条件 `收盘 > 信号价` **永不可能满足** |
| 7-4 | 出现**恒等拒绝** | `603269.SH` 2026-09-01 `收盘27.41 ≤ 信号价27.41` | 与记忆库中 2026-08-25 已修复的「同日确认恒等误拒」同型，但发生在修复之后 |

> ⚠️ **2026-09-25 核对（7-3 / 7-4）**：
> - **7-3 机制未变** —— 确认条件仍是严格 `bar.close > pinfo["signal_low"]`（`bottom_strategy.py:778`）；`signal_low` 来自
>   `_restore_confirm_buffer` 的 `bar_map[ts_code]`（`:286-291`），而该函数每轮用 `_cache_dates[-1]`（`:236`）当「昨日」→ **缓存不刷新时该值恒定**，与观测到的「信号价冻结」一致。
> - **7-4 部分修** —— T+1 守卫在（`:764-768`），但**只覆盖两条路径**：「当日候选」与「DB 恢复且 `signal_time` 非空」。
>   **历史重建兜底路径的 buffer 条目不带 `signal_date`**（`:289-291` 只写 proba/signal_low/weight）→ 被 `if _sig_date and ...` 短路绕过。
>   → **文档称「发生在修复之后」的恒等拒绝，仍有代码通道。**
> - 两者的**具体成因**（缓存陈旧 vs DB `r.price` 陈旧）需查 `signals` 表与 `strategy_decision.log`，本次未连库。
| 7-5 | 交易链路本身通畅 | 近 90 天 5 笔 `executed`，真实券商账户（银河，¥20,560.56，最后交易日 2026-09-11） | 无需处理 |

> 7-3 是**当前正在发生**的问题，且直接导致防守腿（ETF 底部）无法建仓。若该策略将被替换（用户已表示「当前两个策略表现并不好，准备调整」），则可能无需修复。

---

## 八、组合与资金分配专项（2026-09-14 增补）

> 来源：2026-09-14 组合资金链路**全链路追踪**——`CapitalAllocator` → `CompositeService._rebalance_one_group` → `ExecutionService.update_allocated_capital` → `BaseStrategy.resolve_sizing_capital()`，加回测侧 `run_composite`，以及 `shared/market_regime` / `market_state_classifier` 两条 regime 链路。
> **2026-09 五路审计未覆盖组合层**，故本章独立。以下每条的 file:line 均已回代码核实。
>
> ⚠️🔴 **2026-09-25 核对：本章 9 条一条都没动** —— 包括本文档 §十 自己标为「**先行批 P0**」的 8-2 / 8-3 / 8-7。
> `backtest_engine.py` / `backtest_service.py` 在 09-19 确有过提交（`9229cb8`），但九条所指的语义**一处未改**
> （8-3 / 8-7 的行号甚至与文档逐字吻合）。→ **「组合回测结论不可信」这一判断至今成立。**
> 另有两处订正：**8-1 的字面量副本实为 6 处源文件**（非 5 处），且其 fallback **有** `logger.warning`（`capital_allocator.py:216-221`）；
> **8-2 除文档三处外还有第四处** —— `etf/bottom_strategy.py:97,363`（0.03 / fallback 0.01）。
> 逐条状态与行号见 §12-4。

### 8-1 「组合」不是一层能力，是两份 service 里各写一遍的流程 + 5 份默认权重副本

- **现状**：
  - **三条执行路径各自实现**：回测 `BacktestEngine.run_composite()`（`backtest_engine.py:877`）、实盘 `CompositeService._rebalance_one_group()`（`composite_service.py:467`）、卫星 `SatelliteAllocator`（`engines/satellite_allocator.py`，第三套独立规则）。
  - **默认权重 `REGIME_BASE_ALLOCATION` 共 5 处副本**：`capital_allocator.py:57` 类常量（key 为**已不存在的** `etf_bottom`/`stock_low_high`）、`composite_service.py:93-121`、`backtest_service.py:581-607`、`scripts/ops/setup_composite.py:34`、前端 `BacktestWorkspace.vue:509` + `CompositeCenter.vue:386` 各一份。
  - **组合状态变化不经过 EventEngine**：`PortfolioRebalanceEvent` 在 `modules/strategy/events/__init__.py:35` 被注释掉，实盘靠 `strategy_manager.update_strategy_capital()` 直接改运行中策略的内存态。
- **影响**：任何一次权重调整需改 5 处；漏改即静默落到"未知 ID → 权重 0 → 归一化后等权"的 fallback（`capital_allocator.py:216-226`），且**无告警**。回测与实盘权重口径可背离而不被察觉。事件旁路违反 `audit.md`「模块间通信仅通过 EventEngine」。
- **建议**：抽 `shared/portfolio/`（纯计算，无 DB / 无事件依赖）：`RegimeService`（唯一判定入口）+ `AllocationPolicy` 接口（等权 / Regime 查表 / 逆波动率 / 风险平价）+ `constraints`（组合层约束）。回测与实盘只保留"权重 → 资金落地方式"的差异。默认权重单源落 DB，前端只读。补 `PortfolioRebalanceEvent`。
- **验收**：全仓 `REGIME_BASE_ALLOCATION` 字面量只剩 1 处；同一 regime 下回测与实盘输出同一权重（可加断言测试）。
- **优先级**：P1（架构级）。**前置是 8-4 先把 regime 标准定下来**，否则公共化只是把错口径集中到一处。

### 8-2 regime 判定 band 三处不一致

- **现状**：`capital_allocator.py:73` `DEFAULT_PARAMS["regime_gate_band"] = 0.0`；`:186` 取值 `.get("regime_gate_band", 0.03)`；实盘 `composite_service.py:576` **硬编码 0.03**；高波动策略 `high_vol_momentum_strategy.py:126` `annual_gate_band = 0.03`。
- **影响**：组合回测走**二元**判定（band=0）、实盘走 **±3% 三态** → 贴着年线的震荡行情下，回测判 BEAR 取 0.9/0.1 权重行，实盘判 RANGE 取 0.2/0.8。**组合回测结论与实盘行为不可互换引用。**
- **建议**：统一到单一参数（建议 0.03，与策略年线门同源），前端与后端默认生成器都不再各自决定。
- **验收**：同一交易日回测与实盘输出同一 regime；该参数可从一处配置到三处。
- **优先级**：**P0（先行）**——不修则所有组合回测结论不可信；改动面小。

### 8-3 组合回测的 CSI500 无预热 → 窗口前 250 日恒 RANGE

- **现状**：`backtest_engine.py:963` 调 `_load_benchmark_data("000905.SH", start_date, end_date)`，该函数（`:1324-1358`）严格 `BETWEEN :start AND :end`，**不向前多取**；`compute_regime`（`shared/market_regime.py:40-41`）在可用交易日 <250 时返回 RANGE。
- **影响**：① 回测窗口 <250 交易日 → **全程 RANGE**，分配器退化为固定权重；② 窗口 ≥250 时前 249 个交易日仍为 RANGE，且 MA250 只含窗口内数据，与实盘"含完整历史"的 MA250 不是同一个数 → **路径依赖策略的滚动起始日分析被系统性污染**。
- **建议**：加载基准时向前多取 250 个交易日（预热段不参与撮合，只供 regime 判定）；或在 `run_composite` 前单独预取并传入 `csi500_closes`。
- **验收**：回测首日即得到与实盘同源的 regime；6 个滚动起始日首日 regime 不再恒为 RANGE。
- **优先级**：**P0（先行）**，与 8-2 同批。

### 8-4 regime 判定口径本身不准

- **现状（4 条）**：
  1. **两套 regime 并存**：`shared/market_regime.py:11`（CSI500 vs MA250）与 `modules/data/services/market_state_classifier.py`（沪深300 MA20/MA60/slope20 → 写 `market_state_daily.regime`）。后者仍在被消费：`market_router.py:320` 大盘状态雷达、`breadth_service.py:272`、`market_state_service.py:40`、`etf_factor_daily.py:176-207`（映射成 `market_regime` 因子）。2026-08-27 实盘曾切到后者**当天翻车**并改回（`composite_service.py:560-563` 有登记），**但第二套未下线**。
  2. **单指数代表全市场**：CSI500 是中盘指数，而策略实际交易小盘股 / ETF / 全球资产——2024-01 的 54% 回撤根因即为该错配。`shared/market_regime.py:54` 的 `breadth_to_regime()`（全市场站上 MA250 比例）**已写好但全仓零调用**。
  3. **无确认、无 hysteresis**：只有 MA250 ±band 一个条件，无斜率 / 广度 / 量能；`capital_allocator.py:153` regime 一变即重算权重 → 贴年线反复穿越时**资金来回搬**。
  4. **静默降级**：数据不足 250 条 → RANGE（`:40-41`）；`closes[-250:]`（`:43`）取的是"最近 250 条**记录**"而非 250 个**交易日**，缺日会使均线口径漂移且不被拦截。
- **影响**：组合资金方向由单一中盘指数的一年均线决定，与策略实际持仓域错配；两套 regime 并存导致"前端雷达显示的 regime ≠ 组合实际使用的 regime"。
- **建议**：① 明确唯一 regime 标准（多指数 / 广度 / 斜率 / 确认延迟 / 阈值带）；② 给 `breadth_to_regime` 接线或删除；③ 第二套明确降级为"展示用"或合并；④ 加 hysteresis 与缺日检测。
- **验收**：全仓 regime 判定只有一个入口；同一交易日所有消费者得到同一值；缺日 / 数据不足有 WARNING，不静默。
- **优先级**：P1（**是 8-1 公共化的前置**）。

### 8-5 风险平价 P1 全程未实现

- **现状**：`capital_allocator.py:288-301` `_blend_with_risk_parity()` 为 `# P1 TODO`，直接返回原权重；`_vol_buffers`（`:128-131`）创建后**从未被填充**；`risk_parity_enabled` 默认 False（`:67`）→ `_should_rp_rebalance()`（`:264-285`）永不触发，`rp_rebalance_freq` / `rp_blend_strength` / `vol_lookback` 均为**死参数**。设计文档中的"主动再平衡"亦未实现。
- **影响**：权重与策略风险完全脱钩（不区分波动 / 近期表现），风险贡献天然失衡；多个同质动量策略各拿同等资金 = 隐性加倍杠杆（组合层也无相关性输入，见 8-6）。
- **建议**：作为 8-1 的 `AllocationPolicy` 插件实现（逆波动率 → ERC），同时引入策略间相关性（至少做集中度上限）。
- **验收**：策略波动差异能反映到权重；同质策略并存时有相关性约束。
- **优先级**：P2。

### 8-6 组合层风控缺失

- **现状**：`_compute_allocation()`（`capital_allocator.py:194-258`）只做归一化 + clamp，**无**总敞口上限、无重叠标检测、无组合级回撤熔断、无相关性 / 集中度约束。实盘组合模式下 `available_capital = account_total × 权重` 是**分配额而非真实可用现金**（已登记于 `docs/01-业务设计/策略资金与仓位契约.md` §2.4）。
- **影响**：两腿经同一标的或同质风格同时满仓时，组合实际敞口可超 100%（实盘仅靠 broker 资金校验兜底）；回撤熔断只在策略层（各策略自带 ATR 止损），组合层无兜底。
- **建议**：`shared/portfolio/constraints.py` 实现总敞口上限 + 重叠标的检测 + 组合级回撤熔断，回测与实盘共用。
- **验收**：构造"两策略同买同一标的"用例，组合层能识别并约束。
- **优先级**：P2。

### 8-7 注释 / 日志与实现不符（易误导后续判断）

- **现状**：`backtest_service.py:588` docstring 与 `:522` 日志写「熊 0.7/0.3、震 0.5/0.5、牛 0/1」，实现是 `(0.9,0.1)/(0.2,0.8)/(0,1)`（`:600`）；`capital_allocator.py:73` 注释称「与高波动 `annual_gate_band` 对齐」，实际一个是 0.0 一个是 0.03；`capital_allocator.py:179` docstring 写「带 3% 带」，实际取值 0.0。
- **影响**：本次分析中有三处结论曾被注释误导，须回代码核实才纠正；后续审计会重复踩。
- **建议**：注释 / 日志改为与实现一致；更优解是由 8-2 消除参数分叉后删除重复描述。
- **优先级**：P0（与 8-2 同批，改注释零风险）。

### 8-8 `_normalize_and_clamp` 的 [5%, 95%] 钳制不成立

- **现状**：`capital_allocator.py:234-258` 顺序为「归一化 → clamp 到 [0.05,0.95] → **再归一化**」，第二次归一化会使被抬到 5% 的值重新低于 5%。例：`{0.01,0.01,0.98}` → clamp 后 `{0.05,0.05,0.95}` 和为 1.05 → 最终 `{4.76%, 4.76%, 90.48%}`。
- **影响**：docstring 承诺的上下界不生效；小权重策略实际拿到的份额低于设计的 5% 下限。
- **建议**：改为单次带约束的归一化（先 clamp 再按比例分配剩余额度），或放弃下限语义并改注释。
- **优先级**：P3。

### 8-9 组合事件未实现（架构约束）

- **现状**：`modules/strategy/events/__init__.py:35` 为 `# 组合事件 (暂未实现)`，`PortfolioRebalanceEvent` 被注释；实盘 rebalance 直接调 `strategy_manager.update_strategy_capital()` 改运行中策略的内存态。
- **影响**：组合资金变化无事件记录、无审计；违反 `audit.md` 编码前检查「模块间通信仅通过 EventEngine」。
- **建议**：随 8-1 一并事件化。
- **优先级**：P3（并入 8-1）。

### 说明：不在本清单（已登记为有意偏离，**勿当缺陷修**）

> 判定原则同 `策略资金与仓位契约.md` §2.4：偏离必须 **(a) 有明确理由、(b) 有兜底、(c) 已登记**。

| 项 | 登记处 | 说明 |
|:--|:--|:--|
| 跨市场策略 `amount = capital × weight` 不封顶可用现金 | 契约文档 §2.4 | 轮动"先卖后买"时序，按陈旧现金封顶会长期半仓闲置 |
| 组合模式 `available_capital` = 分配额（非真实现金） | 契约文档 §2.4 | 实盘无"每策略现金账户"，按权重拆分为唯一可用近似 |
| 组合回测 1 天时序滞后（用前一日权益） | 契约文档 §四 P2-⑤ | 优于固定 100 万，接受 |
| rounding 少一层 | 契约文档 §四 P2-⑥ | 更精确，接受 |

---

## 九、守卫体系与流程设施专项（2026-09-15 增补）

> **背景**：本项目的红线长期靠「**文档声称 + 人的注意力**」，而非机器强制；而规则里的
> 事实性断言会随代码漂移（同日实测 **8 处「文档声称 ≠ 代码现实」**）。本节记录一次
> 「按外部优质 skill 的机制、补本项目强制层」的整改 —— **抄机制，不装仓库**。
>
> ✅ **2026-09-25 核对：本批是全文档推进得最实的一组** —— **9-1 ~ 9-4、9-9 已完成并逐点复核在案**；
> **9-5 / 9-6 / 9-7 仍未做**；**9-8 部分修**（归整已执行，但「32 个 .py、未跟踪 0」的表述已完全过时 → 实测 **135 个**，`_diag/` 82 个整目录未跟踪）。
> 注意：9-x 的**行号已普遍漂移**（如 `strategy_manager.py` 已移入 `engines/`、`:1255`→`:1259`），定位见 §12-2。

### 9-1 写入守卫有 4 个可绕过的口子 🔴 ✅已修

| # | 漏洞 | 现状 |
|:--|:---|:---|
| 1 | `PreToolUse` matcher 只有 `Write\|Edit` → **`NotebookEdit` 可写 `.env`** | ✅ 扩为 `Write\|Edit\|MultiEdit\|NotebookEdit\|Update` |
| 2 | **`.claude/settings.json` 自身不受保护** → 守卫可被自我修改（业界称"关键难点 1：禁止 agent 改自己的权限定义"） | ✅ 保护 settings.json（**单点总开关**）；hooks 按用户决策放开（见 9-2） |
| 3 | **Bash 写 `.env` 完全不拦**（Bash hook 只匹配 `git push*`） | ✅ 新增 `pre_bash`：抽取**写入目标**再判定 |
| 4 | 路径判定用 `endswith` → **挡不住 `.env.local` / 软链 / `..`** | ✅ 改 `Path.resolve()` + 前缀/基名匹配；相对路径按 payload cwd 与仓库根**双解析**（fail-closed） |

**验证**：判定矩阵 **21/21**（写文件 8 + Bash 8 + 误报 5）；修复前误报 3/5。
**残留**：`pre_bash` 是**启发式** —— 能拦常见 shell 写形态与 `open(...,'w')`，**不等于安全边界**
（当晚误伤 4 次：`2>&1`、`> /dev/null`、命令里含 `> .env` 字样）。已写进 `guard.py` docstring。

### 9-2 守卫的自保护范围是**策略选择**，不是技术问题 🟡 ✅用户已定（选项 B）

把 `.claude/hooks/**` 也锁死会让 agent **无法迭代/修复守卫自身** —— 当晚实测撞死锁 3 次
（发现 pre_bash 误报 → 修不了，因为改 `guard.py` 被 `guard.py` 拦下）。
**决策**：只保护 `.claude/settings.json`（总开关），放开 hooks 代码；hooks 的改动会出现在
`git diff`，**提交时人工 review 是最终兜底**。

### 9-3 🔴 守卫因 **stdin 编码**对含中文的 payload 静默失效 ✅已修

`sys.stdin` 在 Windows 文本模式默认 **GBK**，harness 发的是 **UTF-8** → 含中文的 payload
（**绝大多数真实编辑都带中文注释**）解成乱码 → `json.load` 抛错 → 被 `except` 吞掉 →
返回 `{}` → **守卫静默放行**。

> **症状极具迷惑性**：纯 ASCII payload 一切正常，只有带中文的才漏。本地测试 payload 全是 ASCII，
> 所以一路"验证通过"；直到拿**真实写入**试（含中文 docstring）才暴露，**前三次端到端测试全部漏过**。

**修复**：4 个 hook 一律 `sys.stdin.buffer.read()` + 显式 UTF-8 解码；修后用**含中文 payload** 重测 **6/6** ✓。

### 9-4 ⚠️ 订正：`PostToolUse` 的 `exit 2` **确实会回喂** —— 此前"静默"结论是**误诊**

**先前（本节初版）的错误结论**：依据两次端到端测试（hook 返回 2，但我只看到「文件已创建成功」），
判定「`PostToolUse` 的 `exit 2` 不回喂给模型」。

**真相**：那两次是 **9-3 的 stdin GBK 编码缺陷**导致 hook 在 `json.load` 阶段就 `return 0` ——
**根本没走到 `exit 2`**。修好编码后实测（写含 `shift(-1)` 的策略文件）**收到了清晰回喂**：

```
PostToolUse:Write hook blocking error from command: ".../audit_on_edit.py"
[audit_on_edit] ⛔ ...未通过策略质量门（脚本退出码 1）：<完整质量门报告>
```

> **教训（值得单列）**：把「观测不到 X」直接归因为「不存在 X」，是典型的**把症状当根因**。
> 本例症状（静默）有两个候选根因 ——（a）harness 语义（b）自己的解析 bug ——
> **我跳过了隔离实验就下了结论**。这正是本项目 `memory/feedback-debugging-process`
> 反复强调的纪律：**先验证假设，再下结论/改代码**。
> 附带：同一次测试还暴露出 `audit_strategy.py` 在 GBK 控制台下打印 `🔴` 会
> `UnicodeEncodeError` 崩溃（报告变 traceback、退出码失真）—— 已补 UTF-8 输出。

**`scan_incoming.py`（PreToolUse 版）** 仍保留，但理由已改写：不再因为「PostToolUse 拦不住」，
而是**在写之前拦**（凭证不落盘、不留半成品文件）—— 更早的拦截点，独立成立。
`scan_credentials.py`（PostToolUse 版）继续作为「扫落盘后完整文件」的第二道网。

### 9-5 两个"策略冒烟脚本"根本不加载策略类 🟠

| 脚本 | 加载策略类 | 支持 `[start] [end]` |
|:---|:---|:---|
| `backtest_high_vol_momentum.py` / `backtest_cross_market_momentum.py` | ✅ | ✅ |
| `backtest_etf_bottom.py` | ❌ 独立重实现 | ❌ 日期硬编码 |
| `backtest_small_cap.py` | ❌ 独立重实现 | ✅ |

**后果**：改 `etf/bottom_strategy.py` 或 `microcap/microcap_strategy.py` 后跑它们，**全绿也验证不了策略代码**。
（CLAUDE.md 已订正；自检：`grep -cE "import.*Strategy|from modules.strategy.strategies" scripts/<脚本>.py`，0 = 重实现。）
**待办**：给这两个策略写**真正加载策略类**的冒烟脚本（或把现有脚本改为 import 策略类）—— **尚未做**。

### 9-6 前端工程设施两处名不副实 🟡

| 声称 | 实测 | 待办 |
|:---|:---|:---|
| `pnpm test:e2e # E2E（cypress open）` | **cypress 未安装**、无 `cypress/` 目录、`package.json` 无该依赖 → **E2E 实际不存在** | 补齐，或删该行 |
| 「husky + lint-staged 已配置」 | **只有一半**：`lint-staged` 配置/依赖在，但无 `.husky/`、无 `prepare` 脚本 → git hook 未安装 | `npx husky init`，或删该表述 |

### 9-7 测试与临时文件的收尾 🟡

- **4 个 panic 测试待重写**（相对「ATR 化重构」过时：需灌 ≥15 根 bar 才有 ATR；且假设「固定 -15% 止损」与
  tp1=16%，现口径是 ATR 硬止损 + tp1=20%）—— 重写要点已写在 `strategy-gates.md` 的 skip reason 里
- `tests/modules/test_data/test_services.py` **整类 skip**：测的是 fixture 内自定义的 **stub**（非真实
  `DataSyncService`），对生产**零回归保护** → **建议直接删除**
- 遗留临时文件 `quant_server/_db_17ab4486.py` / `_db_dc862847.py`（DB code dump，2026-08-30）→ 建议删除

### 9-8 `scripts/` 目录归整 🟡（待办）

**现状 76 个 `.py`**：已入库 **29** + 未跟踪 **47**（其中 44 个 `_` 诊断 + 3 个 9-8 新增工具）。

| 类别 | 数量 | 处置建议 |
|:---|:---|:---|
| **A. 正式前缀长期资产（已入库）** | 29 | 保留（`backtest_*` 4 / `backfill_*` 11 / `seed_*` 3 / `sync_margin_hsgt` / `rolling_start_analysis` / `_rolling_start_cross_market` / `password_utils` / `fix_token` / `merge_accounts` / `setup_composite` / `add_strategy_to_composite` / `__init__` / …） |
| **B. 本次新增的 3 个工具（未入库）** | 3 | ✅ **应提交**（正式前缀＝长期资产约定）：`audit_strategy.py`（质量门机检）/ `check_all.py`（收工检查三道门）/ `sync_strategy_code.py`（磁盘↔DB 巡检） |
| **C. `_` 前缀一次性诊断（未跟踪）** | 44 | 分三类（见下） |
| **D. `scripts/_bak/`** | — | `sync_strategy_code.py` 的自动备份目录，可保留；建议加进 `.gitignore` 或定期清 |
| **E. 已标注废弃但仍入库** | 2 | `sweep_params.py` / `v2_param_sweep.py`（commit `6092867` 已标废弃）→ 建议加 `# DEPRECATED` 头或移入 `docs/04-归档/` |

**C 类 44 个的分解与处置**：

| 族 | 数量 | 代表 | 处置 |
|:---|---:|:---|:---|
| `_cm_*`（跨市场实验/对照） | 13 | `_cm_ab_control` / `_cm_arm_problem1` / `_cm_rolling_*.py`（6 个） | 实验已完成；结论已进 `cross-market-strategy-profile` 记忆与代码注释 → **归档后删** |
| `_probe_*`（探针） | 11 | `_probe_etf_bottom_trade_sim`(317 行) / `_probe_live_state_0914*` | 一次性；如认为有复用价值先归档再删 |
| `_phase*`（阶段实验） | 6 | `_phase4_etf_verify`(357 行) / `_phase2_*` / `_phase3_sector` / `_phase4c_benchmark` | 同上 |
| `_diag_*`（诊断） | 4 | `_diag_0911_*` / `_diag_r2_negative` | 同上 |
| `_verify_*` / `_final_*` / `_mutcheck_*` / `_precheck_*` | 6 | `_verify_conclusions` / `_verify_fixes_0914` | 同上 |
| **`_sync_*`（已被取代）** | 2 | `_sync_bottom_code` / `_sync_cm_targeted` | **可直接删** —— 功能已由通用的 `sync_strategy_code.py` 覆盖（`--class <类名> [--apply]`） |
| 其他单件 | 2 | `_strategy_census` / `_analyze_cm_backtest`(536 行) | 同上 |

> ✅ **已于 2026-09-15 执行（用户选项 A）**：归档提交 `17cc54f`（44 文件 / 5924 行）→ 移除提交 `9439eb7`。
> 取回：`git show 17cc54f:quant_server/scripts/<文件名>`。
> 保留未动：`_rolling_start_cross_market.py`（CLAUDE.md 列为长期资产）。
> 同时：3 个新工具已入库（`8d08c9f`）；`scripts/_bak/` 已加入 `.gitignore`。
>
> ✅ **同日二次整理：按类分目录**（`git mv` 保留历史）——
>
> ```
> scripts/  （32 个 .py，未跟踪 0）
> ├── backtest/   6  回测与冒烟（backtest_*×4 · rolling_start_analysis · _rolling_start_cross_market）
> ├── data/      15  回填与播种（backfill_*×11 · seed_*×3 · sync_margin_hsgt）
> ├── quality/    3  质量门与巡检（audit_strategy · check_all · sync_strategy_code）
> └── ops/        7  运维杂项（setup_composite · add_strategy_to_composite · merge_accounts
>                     · password_utils · fix_token · sweep_params⚠️ · v2_param_sweep⚠️）
> ```
>
> **分目录的隐藏成本（实测踩到，值得记录）**：搬文件会**打断脚本内的路径深度假设**。
> 本次共修复 **15 处**，其中 **3 处是"不改就静默/报错"的真 bug**：
> ① `check_all.py` 的 `QUANT_SERVER = SCRIPT_DIR.parent` 会指到 `scripts/`（而非 `quant_server/`）；
> ② 它内部调用的 `scripts/audit_strategy.py` 路径失效；
> ③ `.claude/hooks/audit_on_edit.py` 硬编码的质量门脚本路径失效 → **策略质量门静默失效**。
> 另有 `backfill_adjusted_prices.py` 用 `_SCRIPT_DIR.parent`（**变量名与其余脚本不同**，
> 前两轮 grep 都漏掉，第三轮穷尽扫描才抓到）与 `v2_param_sweep.py` 的 `".."` 相对路径。
>
> ⚠️ **本次还顺带纠正了 §9-4 的一个误诊**（见该节）：此前把「hook 静默」归因为
> 「`PostToolUse` 的 exit 2 不回喂」，实测是 **stdin GBK 编码缺陷**——hook 根本没走到 exit 2。
> 并修掉 `audit_strategy.py` 在 GBK 控制台下打印 `🔴` 崩溃（`UnicodeEncodeError`）的问题。

> **归档先例**：`CLAUDE.md` 已有做法 —— 一次性脚本「先提交入库（如 `450ccf2`）再删」，
> 需要时用 `git show 450ccf2:quant_server/scripts/<文件名>` 取回。**删未跟踪文件不可逆**，故建议照此先例。

---

### 9-9 日志体系：两路分离（实盘决策长期保留 / 系统日志正常清理）✅已完成

**起因**：`logs/` **156M** 平铺 —— 31 个轮转文件 + 22 个诊断产物 + 3 个陈旧残留混在一起；
且 handler 是 `backup_count=30`，即**30 天后删除、不是归档**。

**两个关键发现（决定了设计）**：

1. 🔴 **实盘决策过程不落库** —— `strategy_manager.py:1785-1791` 的「策略每日运行」「`[策略诊断]`」
   只走 `logger.info`；各策略内部的 `[卖出]止损` / `[VETO]` / F9 守卫 / 走弱期同样只打日志。
   **DB 只记「决策结果」（`signals` / `positions` / `orders`），不记「决策过程」。**
   → 要回答「某天为什么没买 / 为什么选它 / 哪道门拒的」，**只能查日志** → **它是唯一记录，不能 30 天删。**

2. ✅ **但 99% 的体积来自回测/故障刷屏，不是决策**：

   | 日期 | 体积 | 构成 |
   |:---|---:|:---|
   | 09-11 | 5.2M | **99.4% 来自 `shared.sources.tushare_source`**（WARNING 24k + ERROR 5.5k）；INFO 仅 **160** 行 |
   | 09-13 | 25.3M | **95% 来自策略层 INFO** —— 其中 **135,501 行是回测/对照实验**（`[跨市场-R²夹零-终验]` 等 draft 实例逐日刷），**实盘仅 1,285 行**（还是首次启动的一次性开销） |

   实盘决策量：**常态 22 行 / 1.6 KB 每天 → 约 0.5 MB/年**。

**实施**：

| 改动 | 内容 |
|:---|:---|
| **新增** `utils/core_utils/logging_utils/decision_log.py` | `live_decision_ctx`（ContextVar）+ `LiveDecisionFilter`（判 **`record.name`** 而非 `record.module` —— 后者是文件推出的模块名，按命名空间过滤会全落空） |
| `main.py:337-380` | 主 handler `backup_count` **30→0**（⚠️ **0 = 本 handler 不删任何轮转文件** —— 若设非 0，它会在超限时**直接删除**最老文件、抢在归档之前毁掉它们）；新增决策 handler（INFO+；**fail-safe**：失败只 warning，不影响启动） |
| `strategy_manager.py:1255` | 在 `_run_live_strategies` **调用处**置位/复位（不改函数体缩进，diff 最小） |
| **新增** `scripts/ops/archive_logs.py` | ① 决策日志按月合并 gzip（**永久**）② 系统日志**满 10 天的 → 按月打成 `archive/system/YYYY-MM.zip`**（**归档非删除**；含 handler 漏掉的 `.1` 变体 —— 实测 `quant_server.log.2026-09-13.1` 达 **51M** 却是当天最大文件，handler 不管它）③ 杂项清理 ④ 陈旧残留移 `archive/legacy/`。**默认 dry-run，幂等** |
| `modules/system/__init__.py` | 注册日终任务 `archive_logs`（**`pre_gate order=1`**，非致命）。⚠️ **不能放 `post_gate`** —— `main_engine.py:241-243` 在数据完整性门失败时会 `return`，**整段 post_gate 被跳过**，而日志归档与数据是否完整无关 |

**验证**（真实装配路径：`HandlerFactory` + root logger 的 `add_handler`）：
回测场景**全不写**；实盘场景**只写策略层 INFO**（DEBUG 被级别过滤、非策略层被命名空间过滤）；**新增恰好 1 行** ✓

**现状与收益**（2026-09-17 实跑）：

| 项 | 结果 |
|:---|:---|
| `logs/` 体积 | **156M → 109M**（回收 47M） |
| `archive/system/2026-08.zip` | 16 个文件 / 42.2M → **2.5M**（**17× 压缩**），完整性校验 ✓ |
| `archive/system/2026-09.zip` | 6 个文件 / 10.7M → **0.7M**（15×），完整性 ✓ |
| 幂等复跑 | 0 个待归档 ✓ |
| `logs/` 目录 | 只剩 `quant_server.log` + 近 10 天轮转 + `strategy_decision.log*` + `archive/` |

**保留策略（最终）**：系统日志**满 10 天 → 按月压缩包（永久）**，不是删除；决策日志永久。
**关键约束**：`main.py` 的 `backup_count` 必须为 **0**，否则 handler 会抢先删除、绕开归档。

**顺带**：09-15 之前的决策日志尚未分流（决策日志 09-15 才独立），那批"决策行"只存在于系统日志里
（如 `dc862847` 高波动 7.1-实盘 / `cebe247d` 熊市防守-01-实盘的每日诊断）→ 已在归档前**保护性抽取**到
`archive/decisions/legacy-2026-08_system-log_decision-lines.log.gz`（668 KB）。⚠️ 该抽取**无法区分实盘/回测**
（回测实例打同样的日志，故含大量回测行，155k 行）—— 采取**宁多勿少**。

**顺带发现（待定）**：`strategy_runs.log_path` —— **全仓无任何代码引用、DB 11 行全部为空**。
设计意图应是"运行记录关联日志文件"但从未实现。**建议**：既然日志已分两路，可考虑让它指向归档路径；**或删字段**避免误导。

---

## 十、建议的执行顺序

| 批次 | 内容 | 理由 |
|:---|:---|:---|
| **先行批** | 8-2 band 口径统一、8-3 回测预热、8-7 注释订正 | 三项直接决定**组合回测结论是否可信**，改动面小、可立即验证 |
| **第一批** | 1-1 版本绑定、1-2 上线门槛、**1-8 磁盘↔DB code 一致性巡检**、1-5 裁决签字、1-6 纸上交易期、1-7 失效降级阈值、1-3 优化器 | 换策略前必须先有**可信的尺子 + 完整的准出流程**（规范已成文：`06_策略实盘准入标准.md`；本批是把规范变成强制）。**顺序：1-1 → 1-2 → 1-8 → 其余** —— 1-1 是 1-2 的前提，1-8 与 1-2 同属"准入时校验对象是否可信"，1-2 是 1-5/1-6/1-7 的载体 |
| **第二批** | 5-1 告警事件错配、6-1 硬编码口令、**5-7 数据完整性门补 ETF/指数** | 低成本、独立、收益确定 |
| **第三批** | 2-1 因子 PIT、**2-6 因子元数据 DB 路径恒失效**、**2-7 因子补注册 + 大小写统一**、**2-8 IC 证据落库**、3-3 标签失真、3-1 模型错配 | 因子家族合并处理（同一批改动，避免反复动 `factor_*`）；2-6/2-7 使「因子注册」形同虚设，成本极低 |
| **第四批** | 1-4 双路径统一、2-2~2-5、3-2/3-4、5-2~5-6、**5-8 质检极值判据、5-9 死配置、5-10 单位守卫、5-11 复权一致性** | 工程完善 |
| **第五批（组合架构）** | 8-4 regime 判定升级 → 8-1 组合公共化 → 8-6 组合层风控、8-5 风险平价、8-8、8-9 | **顺序不可颠倒**：先定 regime 标准（8-4），再抽公共实现（8-1），否则只是把错口径集中到一处 |
| **守卫与设施（2026-09-15 新增）** | **9-1~9-4、9-8、9-9 已完成** ✅ ｜ 待做：**9-5 冒烟脚本补真验证**、**9-6 前端 E2E/husky 补齐或删表述**、**9-7 测试与临时文件收尾**、**9-9 附带的 `strategy_runs.log_path` 死字段（实现或删）** | 与业务逻辑零耦合、成本低；且**9-1~9-4 证明这类"设施层"问题会让红线形同虚设**（守卫因编码缺陷静默失效）。建议紧随第二批 |
| **待确认** | 7-1、7-3、7-4 | 取决于实盘策略是否替换 |

> ⚠️ **2026-09-25 核对后的实际推进情况（明细见第十二章 §12-8）**：
> **先行批零进展**、**第一批仅 1-8 落地一半**（脚本在、未接门禁）、**第二批仅 5-7 部分修 + 6-1 部分修**、
> **第三批仅 2-6 已修**、**第五批（组合架构）9 条一条未动**；
> 守卫与设施批 **9-1~9-4、9-9 确已完成** ✅（9-5/9-6/9-7 未做）。
> → 建议下一轮起手：**① 查库比对 1-8 → ② 8-2 band 统一 → ③ 8-3 基准预热 → ④ 1-1 版本绑定 → ⑤ 9-5~9-7**。
> ⑥ 其余维持原批次顺序（**§八 后半 8-4 → 8-1 → 8-5/8-6 顺序不可颠倒**）。

---

## 十一、本文档的方法论备注

本次审计的**核心教训**：独立审计 Agent 能高效定位代码结构问题，但其**结论性判断**（尤其「因果链」推断）需要回原始数据核验——
本文档第零部分 7 条中有 5 条被推翻或修正，其中 1 条（0-4 僵尸持仓）若直接执行会**删除系统有意保留的数据**。

**因此纪律**：
1. 凡涉及改数据/改代码的结论，先回原始数据（查库 / 读原始代码）核验；
2. Agent 的推断不得直接作为执行依据；
3. 裸 SQL 查询**不等于**系统口径——仓储层（如 `include_zero`）才是契约。

---

## 十二、2026-09-25 全量状态核对

> **方法**：6 路只读 agent 回原始代码逐条复核（HEAD `6e20ae7`，dev 分支），每路负责一个章节。
> ⚠️ **全部结论为静态读码** —— **未连库、未跑测试、未运行代码**。因此「表里有没有数据」「运行时是否真触发」一类断言不在本核对的覆盖范围内（见 §12-7）。
> **判定分四档**：`已修` / `部分修` / `机制已变`（描述变了但风险仍在）/ `仍存在`；`无法核实`（需查库）单列。
> **行号以 2026-09-25 的代码为准**，与本文章节正文所写的行号已普遍漂移（见 §12-6）。

### 12-1 总判定

**57 条：已修 8 · 部分修 9 · 机制已变 2 · 无法核实 3 · 仍存在 35。**

| 层 | 条数 | 占比 |
|:---|---:|---:|
| ✅ 已修 | 8 | 14% |
| 🟡 部分修 / 机制已变 | 11 | 19% |
| 🔴 仍存在 | 35 | 61% |
| ❓ 无法核实（需查库） | 3 | 5% |

**两个「零进展区」值得单独点出**：

1. **第八章（组合与资金分配）9 条一条都没动** —— 包括本文档 §十 自己标为「**先行批 P0**」的 8-2 / 8-3 / 8-7。
   `backtest_engine.py` / `backtest_service.py` 在 09-19 确有过提交（`9229cb8`），但九条所指的语义**一处未改**（8-3 / 8-7 的行号甚至与文档逐字吻合）。
   → **「组合回测结论不可信」这一判断至今成立**。
2. **「可信度与流程」组（1-1 ~ 1-4）全部原样** —— 本文档 §十 标为「换策略前必须先有可信的尺子」的第一批，零进展。
   → 换策略的前置条件没有变化。

> 反过来，**守卫与设施批（§九）是全文档推进得最实的一组**：9-1~9-4、9-9 四条全部落地且经核对确认（这是「基础设施层」，与业务逻辑零耦合、成本低，被优先做掉了 —— 与 §十 的预估一致）。

### 12-2 ✅ 已修（8 条）

| 编号 | 证据（2026-09-25 实测行号） |
|:--|:--|
| **2-6** 因子元数据 DB 路径恒失效 | `research_service.py:891` 改用真实字段 `factor.factor_name`；`:897` `update_frequency`→`factor.calculation_frequency`；`:895-896` 的 `data_source` 键已删；`:905-913` 由宽 `except Exception` 收窄为 `except (AttributeError, TypeError)` 并 **raise**。修复属 commit `a005a14`（2026-09-15） |
| **5-12** 事件优先级被解读反 | `core/engines/system/event_engine.py:47-77` 模块级 `_normalize_priority`（映射表 `:39-44`）；`_to_value:99` 委托之；`put:516` 归一化后再判 `<=2`；出队 `:385` 弹最小值 |
| **5-13** 类式订阅静默失效 | `subscribe:596-621` 非字符串时按 `event_type`→`EVENT_TYPE`→`__name__` 三级回退；`_process_event:425-429` 类名回退（`_cls_key = type(event).__name__`） |
| **9-1** 写入守卫 4 个口子 | matcher 已扩为 `Write\|Edit\|MultiEdit\|NotebookEdit\|Update`；`.claude/settings.json` 在 `guard.py:44-46` `PROTECTED_FILES`；`pre_bash` 在 `guard.py:182-213`（`:222` 分派）；路径判定 `guard.py:99-109` `Path.resolve()` + `:112-133` 双解析 |
| **9-2** 自保护范围 | `guard.py:47-54` `PROTECTED_DIRS` 仅 `.git/`，`.claude/hooks/**` 未锁；docstring `:48-51` 明写选项 B 理由 |
| **9-3** stdin GBK 编码 | 4/4 hook 覆盖：`guard.py:86,91-95`、`scan_credentials.py:62`（`scan_incoming.py:34` 复用）、`audit_on_edit.py:36` |
| **9-4** exit 2 回喂订正 | 双网并存（`scan_incoming.py:68` PreToolUse `return 2` + `scan_credentials.py` PostToolUse）；`audit_strategy.py:38-39` 已补 `sys.stdout.reconfigure(encoding="utf-8")` |
| **9-9** 日志两路分离 | `main.py:354`（主 handler）与 `:381`（决策 handler）`backup_count=0`；`decision_log.py:44,52-63` 按 `record.name` 过滤；`main.py:376,391` 接线；`strategy_manager.py:1259-1265` 置位；`archive_logs` 注册在 `modules/system/__init__.py:129`（`pre_gate, order=1`） |

> 另：`main.py:370` 决策 handler 已于 **2026-09-21** 由日轮转改为**月轮转**（`create_monthly_file_handler`）—— 文档未提，但与设计不冲突。

### 12-3 🟡 部分修 / 机制已变（11 条）

| 编号 | 状态 | 实际状态 |
|:--|:--|:--|
| **1-5** 准出无责任人 | **机制已变** | `06_策略实盘准入标准.md:111-119` 已于 2026-09-21 把「裁决人签字」改为「**独立复审记录**」，**明文不再要求人工签字**；`strategy-reviewer` 仍无否决权、不进任何闸门。→ **作者自证的结构没变，只是不再叫「签字」**。全仓无任何 `*准入决议*` 文件 |
| **1-6** 无纸上交易期 | **机制已变** | 文档已标关闭。核对属实：`06_策略实盘准入标准.md:6-7,29-37,127-144` 为二级实盘（S1 小资金 / S2 大资金）；`:343,372-377` 明写 paper **有意取消**（风险改由资金 ≤20–30% + §五阈值承担），**不是补齐了「上线前观察期」** |
| **1-7** 失效降级无阈值 | 部分修 | 规范 `:320-327` §五 6 条阈值已成文；代码 `strategy_health_service.py:193-258` 有 **8 条告警**（比率 30%/80%/2x/1.5x）—— 但 **口径 ≠ §五 的 1.2×设计MDD / 跑输基准 / 2σ / >50%**，且 **全是告警、无任何自动动作**（`:56,81,194-202` 只产 status 字符串，不写 `strategy.status`、不发事件）。**实盘-回测 2σ 偏差监控零实现**（该文件内 grep「回测」仅命中注释） |
| **1-8** DB/磁盘分叉 | 部分修 | 建议①的脚本**在**（`quant_server/scripts/quality/sync_strategy_code.py`）；但建议②「纳入日终或提交前检查」**未落地** —— 全仓 grep 该名只命中 `CLAUDE.md:42` 与本文档自身，**未接 `.claude/settings.json`、未接 `check_all.py`、未注册任何日终任务** |
| **2-7** 因子注册缺口 | 部分修 | 命名规范**已定**（`03_因子研究标准流程.md:44-51` §1.3 五条）。**补注册与大小写变体清理未做** —— 无 `migrations/`、`scripts/` 内无补注册脚本，`seed_factor_definitions.sql` 最后改动停在 `b1f33a9`（2026-06-24） |
| **5-7** 完整性门不查 ETF | 部分修 | **已明显好于文档描述**：门已扩表 `main_engine.py:293` `_extra_tables = (("index_daily",300),("etf_daily",1000))`，`:339-353` 陈旧/行数不足即 `return False`（注释标 2026-09-21）；另有 `:224,232` 注入点 + `modules/data/__init__.py:321-371` 的 `data_quality_gate`（可阻断）。**但 6 个 pre_gate 任务仍全部非致命**（`:246-251` 逐个 try/except `logger.warning`） |
| **5-9** 质检死配置 | 部分修 | `quality_service.py:73` 新增 `self._quality_config = self.__class__._load_quality_config()` —— **算「有读取方」了**；但 `self._quality_config` 之后**再无任何使用**，阈值 `95.0/90.0` 定义在 `:730-731` 仍无人消费。→ **「配置改了不生效」的结论仍成立** |
| **5-11** 复权一致性 | 部分修 | 唯一实现是只读诊断脚本 `scripts/_diag/_g2_correctness.py:49-84`（判据 `abs_err <= 1e-6`）。⚠️ 该目录 **CLAUDE.md 明文规定「不要提交」** → 实现落在未纳入版本管理的临时脚本里，**未接任何门禁** |
| **6-1** 硬编码口令 | 部分修 | `bottom_strategy.py:28-47` 已改 `_pg_conn_cfg()`（读 `shared.config.config_manager.config.settings.DATABASE`），`:227,312,328` 用 `psycopg2.connect(**cfg)`，该文件 `123456` **0 命中**。**但剩余量已从文档的 20 涨到 50 个文件**，详见 §12-6 第 2 条 |
| **7-4** 恒等拒绝 | 部分修 | T+1 守卫在（`bottom_strategy.py:764-768`：`_sig_date >= _today_s` → 放回 buffer 待次日）；**但只覆盖两条路径** —— 「当日候选」与「DB 恢复且 `signal_time` 非空」。**历史重建兜底路径的 buffer 条目不带 `signal_date`**（`:289-291` 只写 proba/signal_low/weight）→ 被 `if _sig_date and ...` 短路绕过。→ **文档称「发生在修复之后」的恒等拒绝，仍有代码通道** |
| **9-8** scripts 归整 | 部分修 | 归整动作**已执行**（分目录 ✓、`scripts/_bak/` 已在 `.gitignore:28` ✓）；3 处关键路径修复已落地（`check_all.py:40` `SCRIPT_DIR.parent.parent`、`check_all.py:62`、`audit_on_edit.py:58`）。**但文档「32 个 .py、未跟踪 0」已完全过时** —— 实测共 **135 个**：`backtest/` 6 · `data/` 19 · `quality/` 4 · `ops/` 8 · **`_diag/` 82（整目录未跟踪）** · `_bak/` 15 · 根 1；`git ls-files scripts/` = 33 |

### 12-4 🔴 仍存在（35 条）

| 章节 | 条数 | 关键证据（2026-09-25 实测） |
|:--|--:|:--|
| **§一 可信度** | 4 | **1-1** `strategy_service.py:797/809` 两个方法在 `StrategyVersionRepository` 中**仍不存在**（该 repo 只有 `create_version:25` / `get_current_version:159`），`backtest_service.py:376-383` 仍 `logger.debug`；⚠️ 查表方法已换成存在的 `get_current_version`（`:373`），且 `create_version` 定义后**全仓零调用**（死方法）。**1-2** `execution_service.py:171-177` 仍为 warning「仅告警，不阻塞」，全仓无按年化/MDD/夏普拒绝启动的代码。**1-3** `optimization_engine.py:199`（共享实例）`:209`（取第一个有类的类型）`:223`（改同一对象）`:302`（年化用 365）vs `:310`（夏普用 252）；`grid_search.py:59` `asyncio.gather`；`backtest_router.py:630` 路由在册、`config.yaml:56 enable_optimization: true`。**1-4** 4 套成本常量副本零共享（`backtest_high_vol_momentum.py:42-58` 等），脚本侧**不 import** risk/policy 模块，涨跌停/风控脚本侧依旧为零 |
| **§二 因子** | 6 | **2-1** `research_service.py:2352`（索引键仍 `stmt.end_date`）`:2465`+`:2470`（`reindex(method='ffill')` 仍按日历日）`:2332`（过滤仍在，但 `query_date = end_date` 使其形同虚设）。**2-2** IC 用前瞻 `:1505-1506` vs 分组/稳定性用当日 `:1639`/`:1850`。**2-3** `:1363-1366` 无资产类型过滤；`factor_data` 至今**无 `asset_type` 列**（`create_table.sql:3501-3516`）；`z_score`/`percentile`/`rank` 策略侧**零读取方**。**2-4** `backfill_top100_factors.py:359-394` 全样本选池 + 不复权 `research_service.py:1064-1088`。**2-5** `_dict_to_json:238-239` 仍 `pass`；`factor_ic_test.py` `grep -c "def test_"` = **0**；无行业/市值中性化、无正交化（`:1775-1776` 仍只算 `1-mean(\|corr\|)`）。**2-8** 活写回路径 `:2862-2866` **不写** `ic_mean`/`top_minus_bottom`；`handlers.py:2386-2392` 无条件置 `completed`；唯一含 IC 提取的 `factor_research_repo.py:272-317` **全仓无调用方**（死路径，非本次修复产物） |
| **§三 训练** | 4 | **3-1** `bottom_strategy.py:184-198` 仍是 `sorted(model_dir.glob("etf_bottom_v*.joblib"), reverse=True)[0]`；实存 5 个模型，首位 = **`v5_20260816`**（线上仍加载 v5）；无注册表/审批/回滚（全仓 `model_registry\|rollback` 零命中）。**3-2** `training_service.py:227` `ffill().bfill()` + `:228` 中位数填充**均在切分 `:235-237` 之前**；线上 0 填充在 `bottom_strategy.py:409`（该文件 `grep fillna` 零命中）。**3-3** `:221` 仍 `(merged["target"] >= 1).astype(int)`，`:98-100`/`:129-132` 的第三类（回撤条件）**完全无用**。**3-4①** 日终任务清单中**无任何训练任务**，`grep decay\|衰减\|drift\|漂移` 零命中；**3-4②** `lightgbm`/`scikit-learn`/`joblib` **仍不在** `pyproject.toml`（`:24-56` 与 `:62-77` 均无），而 `training_service.py:22-27`、`bottom_strategy.py:17` 直接 import |
| **§五 监控** | 8 | **5-1** 发布 `risk_events.py:118` = `risk.alert.triggered` vs 订阅 `alert_engine.py:75` = `monitor.risk.alert.triggered`（文件已从 `services/` 迁到 `engines/`），全仓无订阅者。**5-2** `risk_engine.py:613-616` 守卫仍要求 `rule_id`/`user_id`，而 violation 字典 `:546-556` 仍不含。**5-3** `wechat_alerter.py:65` 仍只判 `resp.status == 200`，无 `errcode` 解析（对照 `dingtalk_alerter.py:70` **已**解析 → 两通道口径不一致）。**5-4** 全仓 `grep EmailAlerter` 零命中；`alert_engine.py:87-130` 只有 wechat/dingtalk 两支，`:95` 读入的 `email_enabled` **从未使用**。**5-5** 双实例仍在（`modules/risk/__init__.py:81-100` 与 `modules/trade/__init__.py:131-161`；后者已是 19 行兼容壳，但**实例仍是两个**），两端**都订阅** `risk.check.requested` → 同一次检查跑两遍。**5-6** `signal_repo_v2.py:41` 全仓无调用方（同文件另两个函数有）。**5-8** `quality_service.py:789-790` 仍统一 `\|close-open\|/open\| <= 0.2`，全文不含 `stock_daily_limit`。**5-10** DDL 三单位混用（见 §12-6 订正） |
| **§七 实盘** | 1 | **7-3** 确认条件仍严格 `bar.close > pinfo["signal_low"]`（`bottom_strategy.py:778`）；`signal_low` 来源 `_restore_confirm_buffer`（`:286-291`）用 `_cache_dates[-1]`（`:236`）当「昨日」→ 缓存不刷新时该值恒定，与 7-3 观测一致 |
| **§八 组合** | **9** | **全部原样**。**8-1** 字面量副本实为 **6 处源文件**（`capital_allocator.py:57-62`、`composite_service.py:109-116`、`backtest_service.py:600-607`、`setup_composite.py:34-38`、`BacktestWorkspace.vue:509-514`、`CompositeCenter.vue:385-390`），`shared/portfolio/` 未抽出，`:59-61` 仍是 `etf_bottom`/`stock_low_high`。**8-2** band 仍三处不一致（`capital_allocator.py:73` 0.0 / `:186` 默认 0.03 因置键恒不生效 / `composite_service.py:576` 硬编码 0.03 / `high_vol_momentum_strategy.py:126,187` 0.03），且**回测侧 `grep regime_gate_band` 零命中** → 回测恒走 0.0 二元。**8-3** `backtest_engine.py:970` → `_load_benchmark_data:1337`，三源 SQL 全为 `BETWEEN :start AND :end`（`:1363,1371,1378`），无预热。**8-4** 两套 regime 并存且第二套日终仍在写（`modules/data/__init__.py:296-303`），消费方含 `market_router.py:321`、`breadth_service.py:266-272`、`market_state_service.py:35-40`、`etf_factor_daily.py:176-207`、**`microcap_strategy.py:154,374`**（文档未列）；`breadth_to_regime` 调用方**只在未跟踪的 `_diag/`**；无 hysteresis、`len<250 → return 1` 静默。**8-5** `capital_allocator.py:300-301` 仍 `# P1 TODO` 直接 return；`_vol_buffers` 创建后无任何 `.append`；`risk_parity_enabled:67` 默认 False。**8-6** `_compute_allocation:194-232` 无总敞口/重叠/熔断/相关性（grep `constraints\|overlap_detect\|drawdown_circuit` 零命中）。**8-7** `backtest_service.py:588` docstring + `:522` 日志仍写「熊0.7/0.3 震0.5/0.5」vs `:600` 实现 `(0.9,0.1)/(0.2,0.8)/(0,1)`；`capital_allocator.py:73`/`:179` 注释与实现不符。**8-8** `:234-258` 仍「归一化→clamp→再归一化」。**8-9** `modules/strategy/events/__init__.py:36` 仍 `# "PortfolioRebalanceEvent",` |
| **§九 设施** | 3 | **9-5** 自检实测 `backtest_etf_bottom.py`=**0**、`backtest_small_cap.py`=**0**（high_vol/cross_market=1）；`scripts/backtest/` 无新增的真加载策略类的冒烟脚本。**9-6** `quant_web/package.json:10` 有 `"test:e2e": "cypress open"` 但 devDeps **无 cypress**；无 `prepare` 脚本；`.husky/` 与 `cypress/` 目录均不存在（`husky ^9.0.11` 已在 devDeps 但未 init）。**9-7** 4 个 panic skip 仍在（`test_panic_bottom_strategy.py:92,152,176,201`）；`tests/modules/test_data/test_services.py:44` 类级 skip 仍在；`quant_server/_db_17ab4486.py`（62 KB）/ `_db_dc862847.py`（50 KB）**仍在仓库根** |

### 12-5 ❓ 无法核实（3 条，需查库）

| 编号 | 需查什么 |
|:--|:--|
| **7-1** | `strategies.status` 的真实分布（`paused` 语义是否为「不拦截信号生成」） |
| **7-2** | `signals` 的近 90 天拒绝原因分布（70% 被拒的构成） |
| **7-5** | `orders` 的 `executed` 计数与账户余额 |

> **§三 3-4③ 亦属此类**（「DB `strategy_parameters` 架空代码收紧值」）—— 仓库内**无任何** `INSERT INTO strategy_parameters`，DDL 只有结构无数据，唯一的行来源是 `template_service.py:181-190` 建实例时复制模板默认参数（**历史值不可从仓库还原**）。
> ⚠️ 且该条的**因果链需重写**：即使该行存在，按代码路径它**只在回测生效、对实盘无效** —— 实盘不合并 DB 参数（`strategy_manager.py:513-518` 与 `:2235` 恢复路径**都传 `parameters={}`**，`:533-542` 仅比对打日志；前端启动时**不发 `parameters`**），回测才合并（`backtest_service.py:1983-2002` → `:1494-1500` 注入）。文档断言「从未生效」的方向与代码相反。
> 查库现成查询：`scripts/_diag/_live_report_data.py:61` 的 `SELECT param_name, param_value FROM strategy_parameters WHERE strategy_id=$1`。

### 12-6 ⚠️ 本文档自身的失真订正（7 处）

> 以下为核对中发现**文档正文写错**的地方 —— 读对应章节时会直接被误导，故在正文处也已就地加注。

| # | 位置 | 文档原话 | 实际情况 |
|:--|:--|:--|:--|
| 1 | **5-10** | 「`etf_daily`/`index_daily` 是**万元**」（09-15 那次订正） | ❌ **该订正本身是错的**：DDL 实测两张都是**千元**（`etf_daily:3243`、`index_daily:3319`）。`04_回测分析标准流程.md:69` 已于 09-19 明写「09-15 那次订正把这两张改错了」→ **两份标准文档互相矛盾，且后者指向前者错了** |
| 2 | **6-1** | 「`modules/market/services/limit_service.py`（§6-1 漏列的另一处生产服务）」；「仍有 20 个文件」 | ❌ **假阳性**：该文件 `grep -i password` **零命中**，命中的是 `:219` 的 `"seal_amount": 1234567.8`（`.8` 被 `123456` 子串匹配）；`git log -p --all` 该文件历史中**从未出现 password**。**计数也已过时**：现为 **50 个文件**（`_diag/` 35 + `data/` 9 + `ops/` 4 + `backtest/backtest_etf_bottom.py` 1 + `market_state_classifier.py:40` 1），另加文档未列的 `shared/config/config_manager.py:60` `DatabaseSettings.PASSWORD: str = "123456"` 兜底默认值 |
| 3 | **5-5** | 「前端操作 **trade 侧**、`SignalEngine` 持 **risk 侧**」 | ❌ **方向写反**：按 `main.py:764 _sort_modules_by_dependency` 的拓扑序（risk 在 trade 之后）**risk 侧后写覆盖** `_module_engines["risk_engine"]` → 实测**前端开关打到 risk 侧**、`SignalEngine`（`trade/__init__.py:150-156`）持 **trade 侧**。**结论（前端关规则对实盘无效）仍成立，但因果链相反** |
| 4 | **3-1** | 「`model_trainings` 表 **0 行** → 训练 API 从未成功执行」 | ⚠️ **不能由代码层断定**：写入路径**存在** —— INSERT 在 `api/routers/strategy_router.py:966-1008`（`:971` 建行、`:987` commit），UPDATE 在 `training_service.py:340-352`。行数需查库。另：`training_service.py:286` 硬编码 `etf_bottom_v2_{today}.joblib` → 训练**永远产不出 v3/v5**；`grep -a label_type` 实测 v2/v3=1、**v5=0**，`n_train` v1/v2/v3=1、**v5=0** |
| 5 | **2-5** | `factor_definition_repo.py` / `factor_ic_test.py`（无路径） | ⚠️ **路径写错**：实为 `shared/database/repositories/analysis/factor/factor_definition_repo.py`（在 `analysis/factor/` 子目录下）与 `tests/modules/test_strategy/factor_ic_test.py`。**缺陷本身仍存在** |
| 6 | **8-1** | 「默认权重共 **5 处**副本」 | ⚠️ **实为 6 处源文件**（另 `quant_web/dist/assets/*.js` 内 2 处为构建产物）。另文档称 fallback「**无告警**」亦不准确 —— `capital_allocator.py:216-221` 实际有 `logger.warning` |
| 7 | **全文档** | 各处 `file:line` | ⚠️ **行号已普遍漂移**（本文写于 09-12~09-19，此次核到 09-25 HEAD）：`research_service.py` 整体后移约 8 行、`bottom_strategy.py:161-175`→`:184-198`、`backtest_engine.py:963`→`:970`、`quality_service.py:594-605`→`:789-790`、`strategy_manager.py` 已移入 `engines/`、`alert_engine.py` 已移入 `engines/`、`StrategyWorkspace.vue` 已移入 `views/StrategyCenter/`。**照文档行号定位会找错地方** |

### 12-7 证不了什么（本次核对的边界）

全 6 路**均未连库、未跑测试、未运行代码**，结论全部是**静态读码**。因此以下**没有**被验证：

1. **所有「表里有几行 / 是不是空」的断言** —— `strategy_versions` 是 0 行（本文档 1-1）还是 7 行（`06_策略实盘准入标准.md:340` 自记）、`risk_events` 是否恒空、`factor_research` 的 `ic_mean`/`top_minus_bottom` 是否全空、`factor_data` 的 102 vs `factor_definitions` 的 83、`model_trainings` 行数。代码层只能证明「写入路径的形状」，**不能证明表里的数据**。
2. **1-8 的分叉现状** —— `sync_strategy_code.py` **存在 ≠ 当前无分叉**。需连库取 `strategies.code` / `strategy_templates.code_template` 与磁盘 diff。**这条直接决定主池进攻策略（高波动 7.1）恢复运行是否安全**。
3. **8-2 的实际严重度** —— 未查 `composite_groups.allocator_config` 的真实值。若线上 DB 配了自定义 `REGIME_BASE_ALLOCATION` / `regime_gate_band`，结论会不同。另需注意仓库根有 `_db_17ab4486.py`（其 `annual_gate_band: 0.0` 与磁盘 0.03 不同）、`_db_dc862847.py` 两个疑似 DB 代码快照 —— **它们是否等于当前 live 实例无法核实**，故 8-2 的 band 分叉**可能比代码层看到的还多一处**。
4. **策略的运行时形态** —— 本项目策略代码从 DB `strategies.code` 加载。本次只核了**磁盘文件**：DB 里 3 个实例 + 1 个 template 是否也清了 4 处 `123456`、是否也带 `signal_date` 守卫，**磁盘结论不覆盖 DB**。
5. **7-3 / 7-4 的具体成因** —— 只证明「机制上仍能发生」。实际是哪条路径触发（缓存陈旧 vs DB `r.price` 陈旧）、`513050.SH` / `603269.SH` 的确切成因，**必须查 `signals` 表与 `strategy_decision.log`**。
6. **9-1 的「21/21 判定矩阵」未复现** —— 只静态确认 4 个修复点在位。**「能拦住」≠「已拦住」**。
7. **未跑事件引擎测试**复现优先级出队顺序；**未加载任何 `.joblib`** 独立验证 v5 的 53 特征 / 0.48 阈值（3-1 的「线上加载 v5」是对 `glob + reverse=True` 与目录内容的**推断**，非观测 —— 若 DB 副本的 `model_path` 被别的路径注入，结论会变）。
8. **`_diag/` 82 个脚本的用途未逐一判定** —— 只统计了数量与未跟踪状态，其中是否含真实新缺陷线索未看。
9. **历史时点不可复原** —— 文档写于 09-12~09-19，本次核的是 09-25 HEAD（`6e20ae7`）。若某条恰在 09-19~09-25 之间被改又改回，本核查看不出来。

### 12-8 建议的下一步（按核对结果重排）

原 §十 的批次划分**方向仍成立**，但对照实际推进情况，建议这样起手：

| 优先级 | 事项 | 为什么是它 |
|:--|:--|:--|
| **① 查库比对 1-8** | 取 DB `strategies.code` 与磁盘 diff | 主池进攻策略（高波动 7.1）恢复运行的前置；纯只读、当日可完成；且是 1-2 准入校验的前提 |
| **② 8-2 band 统一** | 三处（+ 新发现的第四处 `bottom_strategy.py:97,363`）收敛到 0.03 | 先行批里**改动面最小**，直接决定「组合回测结论是否可信」；不修则所有组合回测结论不可引用 |
| **③ 8-3 基准预热** | `_load_benchmark_data` 向前多取 250 交易日 | 与 ②同批；不修则滚动起始日分析被系统性污染 |
| **④ 1-1 版本绑定** | 实现缺失的两个方法或每次编辑建快照 | 1-2 准入的**前提**（准入判断的对象是「某次回测结果」，无版本绑定则「达标」不可复现） |
| **⑤ 9-5 ~ 9-7** | 冒烟脚本补真验证 / 前端设施补齐或删表述 / 测试临时文件收尾 | 与业务逻辑零耦合、成本低；且 §九 已证明这类「设施层」问题会让红线形同虚设 |

> **不建议现在动的**：§八 后半（8-4 → 8-1 → 8-5/8-6）顺序不可颠倒 —— 先定 regime 标准（8-4），再抽公共实现（8-1），否则只是把错口径集中到一处。
