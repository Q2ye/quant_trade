# 16 — 全量修改终审报告（2026-08）

> 范围：A/B/C 类修复 + C4/C15/C17 + D 批死代码清理（D1~D12）全程改动。
> 终审方式：残留文件扫描、全库引用残留 grep、py_compile、全链路导入冒烟、全量 pytest、两次真实回测对账。

## 一、修改总量

| 类别 | 内容 | 规模 |
|:---|:---|:---|
| A 类修复 | 事件引擎/仓储/认证/结算等 39+ 项 | 40+ 文件 |
| B 类防御 | 认证/脱敏/限流/背压等 15 项 | 20+ 文件 |
| C 类口径 | C1~C16（含 C4 绩效口径、C14 pnl 回填、C15 依赖反转）+ **C17 复权活路径统一** | 20+ 文件 |
| D 批删除 | D1~D12 全 13 组：utils/core/shared/7 模块后端 + 前端 91 文件 | **220+ 文件删除**，净减近万行 |
| 流程新增修复 | 4 处未 await 事件发布、LGBM 预测路径、BacktestEngine 抽象类事故 | 5 文件 |
| 测试新增 | C4 回归 12 项、C17 一致性 5 项 | tests/ 2 文件 |

## 二、验证矩阵（全部通过）

| 验证 | 结果 |
|:---|:---|
| py_compile | 每批全文件 ✅ |
| 全链路导入冒烟（`import main, api.main`） | 每批 exit=0 ✅ |
| 全量 pytest | **82 passed / 9 failed / 4 skipped / 31 errors**——失败/错误画像与任务开始前基线**逐项一致**（9f/31e 全为既有问题：pytest-asyncio 缺失、EventEngine 旧签名测试、industry_rotation 引用已迁移文件） |
| 真实回测对账 | ETF 底部 `92cd7a40`→`1e5cad00` 逐项一致（1.17%/-0.50/0.82%/6 笔）；低吸轮动 `80cf21de`→`5b2a98e3` 逐项一致（62.88%/1.88/26.67%/109 笔/胜率 28.4%）✅ |
| BacktestEngine 实例化 | `__abstractmethods__ == frozenset()` ✅ |

## 三、终审发现与处理

| # | 发现 | 处理 |
|:---|:---|:---|
| 1 | quant_server 根目录残留 15 个 `pytest_*.txt` 调试文件 | 已清理 |
| 2 | 已删符号残留引用全库 grep | 仅剩 **注释/docstring/字符串级** 无害命中（`enums.py:305/307` EngineType 孤儿成员、`backtest_service.py:171/173` docstring、`result_events.py:51` source 字符串、`progress_events.py:573` 字符串）——**无悬空 import** |
| 3 | `pyproject.toml` | 依赖收敛正确（pyqt5 系/seaborn/matplotlib 已删，reportlab 保留） |
| 4 | 临时脚本 | 全部已删（`_batch*`/`_fix*`/`_migrate*` 零残留） |

## 四、已知遗留风险（按优先级）

1. **🔴 策略实例与磁盘模板代码分歧**：实盘/回测策略实例走 DB `strategies.code` exec 路径（日志 `策略类加载成功 (exec)` 实证）——磁盘上的 LGBM 修复、死成员清理、历史 C 类策略修复**对现有实例不生效**，必须通过前端「策略版本」页发布新版本后才会生效。发布后建议复跑一次回测确认。
2. **🟡 C15 日终 pipeline 未观测**：三处 `register_daily_task` 已迁入活路径（py_compile+冒烟通过），但**下一个日终（21:00）的 `_run_daily_pipeline` 执行日志尚未被观察到**——首次运行时请重点看：pre_gate（sync_daily→market_state_update→etf_factor→daily_settlement）→ 完整性门 → post_gate（composite_rebalance→strategy_drive）的完整链日志。
3. **🟡 前端 build 未验证**：批 5 删了 91 个前端文件（删除后 grep 零残留引用），但 `pnpm build` / `vue-tsc` 未跑——构建是最终裁决。
4. **🟡 git 未提交**：批 4a~6 + C17 + 事故修复均未 commit；其中 `password.py` 与 `backtest_engine.py` 经历过"git 恢复→重新修改"，提交前需核对最终状态（password.py 应为 207 行精确签名版；backtest_engine.py 应为 1633 行且含生命周期三方法）。
5. **🟢 C14 DDL**：`signals` 表 `is_executed`/`pnl_outcome` 两列迁移此前由你执行，本次未复核——抽空确认 `SELECT column_name FROM information_schema.columns WHERE table_name='signals'` 含这两列。
6. **🟢 功能收缩记录**：portfolio 假数据 API 整链、前端 3 个绩效页面、metrics 参数已按决策删除（14 清单有记录）；如未来需要组合管理/绩效对比页，需按新需求重建。

## 五、提交前检查清单

- [ ] `password.py` 最终状态正确（含 PasswordManager.encrypt/verify、get_password_crypto，无模块级死函数）
- [ ] `backtest_engine.py` 最终状态正确（1633 行、生命周期三方法在位、旧接口 8 方法不在）
- [ ] 分批 commit（建议：批 4a / 4b / 4c / 批 5 前端 / 批 6 三波 / C17+对账 / 终审清理，共约 7 个 commit 边界）
- [ ] 前端 `pnpm build` + 可选 `vue-tsc --noEmit`
- [ ] 策略版本发布（运营）
- [ ] 观察下个日终 C15 pipeline 日志

## 六、结论

代码侧工作全部完成且经静态+运行时双重复核：pytest 画像零变化、真实回测双项对账逐项一致、全链路导入冒烟每批通过。剩余事项均为**用户侧操作**（git/前端构建/版本发布/日终观察），无待改代码。文档链路完整：`11-系统级修复实施方案.md`（进度指针）、`14-D批死代码引用验证清单.md`（170+ 目标全记录+对账结论）、`15-C17复权口径活路径统一方案.md`（实施记录）、本报告（终审）。
