# quant_trade 代码全面 Review 总报告

> 2026-08 | 覆盖后端 quant_server（core/shared/modules/api/utils）+ 前端 quant_web 全部代码
> 审查依据业界标准：Google Code Review（Code Health）、Clean Code、SOLID、Martin Fowler《重构》坏味道清单、OWASP Top 10/API Security、PEP 8、Vue 3 官方风格指南、量化回测规范、交易系统规范（OMS 订单状态机/幂等）、券商清算最佳实践、GIPS 绩效口径、Alertmanager 告警规范
> 分报告：`01` core ｜ `02` shared ｜ `03` data+market ｜ `04` strategy+backtest ｜ `05` trade+risk ｜ `06` account系 ｜ `07` api ｜ `08` 前端 ｜ `09` 全局架构与工程卫生 ｜ `10` 校准说明
> **状态：✅ 9/9 分报告全部完成，本总报告已汇总完毕**（223+ 项问题：严重 2 / 高 112 / 中 97 / 低 12，P0 高危 60 项）
>
> ⚠️ **2026-08 业务模式校准**（详见 `10-review报告校准说明.md`）：本系统为**半自动交易**（信号→人工券商交易→**手动录单** `/trades/record`→系统读单），主链路为录单链路；且处于资金膨胀期、风控从宽。
> 据此校准：① 录单主链路经核实**健康正确**（7 步事务闭环+持仓/资金校验+信号回写）；② §1.1 中针对"自动执行链路"的问题（P0-1/2/4/7/8）降级为"自动模式预备批次"；③ 风控阈值宽松（P0-5）为**设计意图**，修复方向改为清理死常量而非收紧；④ 半自动闭环（P0-3）修正为"录单 signal_id 防呆"中等问题；⑤ 修复批次顺序按校准说明 §3 执行。

---

## 一、已发现 P0 高危问题（跨报告去重，必须优先修复）

### 1.1 交易正确性（来源：05 报告）

> ⚠️ **已按半自动模式校准**：P0-1/P0-2/P0-4/P0-7/P0-8 降级为"自动模式预备批次"（半自动主链路不走 ExecutionEngine）；P0-3 修正为"录单 signal_id 防呆"（中）；P0-5 修正为设计意图（修复=清理死常量）；P0-6 维持（盈亏回填缺口真实存在）；P0-9 维持中高（防御）。详见 `10-review报告校准说明.md`。

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-1 | **引擎成交不落库**：ExecutionEngine 未注入 session_factory，模拟路径不写 DB，实盘路径条件 commit → 引擎下单后 orders/trades/positions/accounts 全无记录，持仓/资金零变化 | `execution_engine.py:162-240`、`trade/__init__.py:140-147` | 注入 session_factory；成交链（order→trade→position→account）事务化落库 |
| P0-2 | **资金校验实际为 0 层**："三层防护"注释误导，模拟买入可致负资金 | `execution_engine.py:162-240`、`sim_adapter.py:98-115`、`account_rules.py:33-39` | 引擎入口统一资金/持仓校验（买入金额+费用≤可用；卖出持仓≥数量） |
| P0-3 | **半自动确认闭环断裂**：review_signal 仅改 `signal_status=approved` 不生成订单不发事件；`approved` 状态无任何消费者 | `handlers.py:610-652`、`signal_router.py:37-86` | 统一确认链路：approve → 生成订单 → 成交 → 回写 |
| P0-4 | **execute_signal API 假执行**：直接写 submitted 订单即返回"执行成功"，无撮合无持仓无资金变动，signal_id 硬塞成 order_id | `handlers.py:303-363` | 删除或改走 SignalEngine.process_signal 真实链路 |
| P0-5 | **风控阈值实际生效比声明宽松 3 倍**：规则按类默认无参实例化，实际 0.5/0.15/0.25（单股 50%、亏损 15%、回撤 25%），constants 的 0.30/0.05/0.10 全是死值 | `position_rules.py:67`、`account_rules.py:45,102` vs `risk/constants.py:57,62-63` | 阈值单一事实源注入规则，删除死常量 |
| P0-6 | **pnl_outcome 盈亏回填不存在**：Signal 无 pnl 字段，全库无 `pnl_outcome` 引用——设计文档声称的"信号→盈亏闭环"（数据资产核心）实际未实现 | Signal 模型 `business_models.py:247` | 增加 signal 盈亏回填字段并接入 round_trip 结算 |
| P0-7 | **涨跌停规则信号链路永不触发**：信号 dict 无 `pre_close`，规则内 `pre_close=close` → 永不到限价；模拟成交也不校验涨跌停 | `market_rules.py:174-195`、`signal_engine.py:181-199` | 信号注入 pre_close/is_st；模拟撮合前校验 |
| P0-8 | **双 RiskEngine 实例**：trade 与 risk 模块各建一个，都订阅 risk.check.requested、都跑巡检 → 风控检查×2、违规事件×2 | `trade/__init__.py:131-137`、`risk/__init__.py:81-117` | 统一单实例，从 main_engine 注册表取用 |
| P0-9 | **SIMULATED_TRADING 可运行时关闭**：update_trading_config 可在运行中改为 false 且无审计 | `trade_manager.py:40-52` | 禁止运行时切换，需重启+审计 |

### 1.2 安全（来源：07 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-10 | **信号确认/取消/待确认/追溯全部无认证**——任何人可确认成交（资金级操作） | `signal_router.py:37-156` | 全部加 get_current_user + 交易权限 |
| P0-11 | **WebSocket 无鉴权且可订阅订单/持仓/账户频道** → 泄露全系统实时敏感数据 | `websocket/routers.py:17-21` | 连接鉴权（JWT）+ 频道权限过滤 |
| P0-12 | **logout 失效**：固定传 `token="unknown"` 进黑名单 | `system_router.py:518-524` | 从 Authorization 头取 token |
| P0-13 | **500 错误泄露内部异常**（30+ 处 `HTTPException(detail=str(e))`） | market/account/composite/backtest/template/system 等 | 统一"服务器内部错误"，str(e) 仅入日志 |
| P0-14 | **run-scenario 任意代码执行无沙箱**（后台线程池） | `backtest_router.py:664-728` | 限定并发 + 资源限额；长期沙箱化 |
| P0-15 | 告警管理恒 403（读不存在的 `can_manage_alerts` 字段）；自选股用错键（`user_id` vs `id`）→ 功能失效且全用户共享；交易统计无 user_id 过滤跨用户泄露 | `monitor_router.py:221-329`、`market_router.py:340,354`、`trade_router.py:668-718` | 修键名 / 加过滤 |

### 1.3 架构（来源：09 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-16 | **core 反向依赖 modules**（6 处，违反依赖方向红线） | `core/engines/system/main_engine.py:221-340` | 回调注册表/事件机制解耦 |
| P0-17 | **核心交易链路零测试**（trade/risk/backtest 8 模块）+ 前端 4 个测试文件 | tests/、quant_web/tests/ | 补单测（trade>risk>backtest 优先） |

### 1.4 引擎基座（来源：01 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-18 | **事件优先级反转**：QueuedEvent 字符串优先级字母序排序，BACKGROUND 反而最先处理 | `event_engine.py:38-64` | 排序键改数值 |
| P0-19 | **队列满时 CRITICAL 事件按 NORMAL 处理被丢弃**（BaseEvent 无 .priority 属性） | `event_engine.py:462-501` | 从 metadata.priority 取优先级 |
| P0-20 | **风险事件永不发布**：async put 未 await（risk_engine/risk_manager 3 处） | `risk_engine.py:358,374`、`risk_manager.py:150` | 补 await |
| P0-21 | **integration_service 调用不存在的 publish** → AttributeError 必现 | `integration_service.py:127,205,293` | 改 await put |
| P0-22 | **引擎工厂循环依赖死锁**（持锁递归） | `engine_factory.py:445,512,781` | 依赖解析移出锁 |
| P0-23 | 约 3000+ 行异常体系死代码 + 错误码撞值（"5001"/"1006"） | `core/exceptions/`、`error_codes.py:105,157` | 按引用清单删除/唯一性校验 |

### 1.5 前端（来源：08 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-24 | **WS 实时推送 commit 到不存在的 store 模块** → 行情/信号/告警实时推送全部失效 | `composables/useWebSocket.ts:83-124` | 统一 api/websocket.ts 单例重写消息路由 |
| P0-25 | **401 处理不完整**：只清 localStorage 不清 store、无 token 刷新、整页跳转丢状态 | `utils/request.ts:104-115` | 清 store + refresh token 重放队列 |
| P0-26 | **假订单提交**：OrderForm"提交成功"无 API 调用（幽灵订单） | `components/trade/OrderForm.vue:35-41` | 整链删除（死代码） |
| P0-27 | **前端死代码规模大**：8 视图 + ~50 组件 + 6 store 模块 + persistedstate 插件 + StrategyLayout/locales 均无引用（约可减 30% 源码） | `views/`、`components/`、`store/` | 按 08 §2 清单批量清理 |

### 1.6 策略与回测（来源：04 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-38 | **exec"沙箱"可 RCE**：保留 `__import__`，启动时自动执行 DB 中策略代码 | `strategy_manager.py:183-199` | 移除受限 import + 代码审批机制 |
| P0-39 | **每日绩效恒写 total_assets=0** → 幽灵 -100% 绩效 | `performance_tracker.py:83` | 传入真实总资产 |
| P0-40 | **参数优化 objective 恒 -inf**，优化结果无意义 | `optimization_engine.py:199-211` | 注册策略类到优化引擎 |
| P0-41 | **回测佣金/滑点配置被忽略**（任务级配置未生效） | `backtest_engine.py:415-421` | run 前覆盖 broker.config |
| P0-42 | **同日两笔卖出超卖**：第二笔无持仓校验，现金虚增 | `backtest_broker.py:1266-1277` | 成交时持仓数量校验 |
| P0-43 | **NaN 污染账本**（回撤保护永久失效）；**entry=0 除零持仓永不退出** | `stock_low_high_strategy.py:1207-1211`、`bottom_strategy.py:636,659` | isfinite/价格守卫 |
| P0-44 | **回测不可复现**：版本快照记录但执行用当前代码 | `backtest_service.py:379-394 vs 1283` | 执行时读快照代码 |
| P0-45 | **裸信号按 100% 资金买入**（weight 默认 None 无 Sizer 兜底）；**T+1 可卖校验缺失** | `models.py:99`、`sizer.py:209-218`、`backtest_broker.py:313-332` | weight 默认 0 + available_quantity 校验 |
| P0-46 | **SQL 注入 + 硬编码口令**（训练服务） | `training_service.py` | 参数化 + 清理凭证 |

### 1.7 数据正确性（来源：03 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-28 | **data_sync_tasks 缺 parent_task_id 列** → 批量同步/取消/任务列表全链路 UndefinedColumn（新库必现） | `create_table.sql:1396-1415` vs `business_models.py:939` | 补列 + 同步 DDL |
| P0-29 | **PIT 正确性破坏**：停牌日前向填充假行情、除权跳空被异常值覆盖、以最新价回填 | `clean_service.py:1267-1316,1403` | 停牌留缺失 + pre_close 基准 + 前一日价 |
| P0-30 | **后复权公式错误 + 前复权增量写入锚定漂移** | `adjusted_price_generator.py:126-140` | 修正公式 + 全表重锚 |
| P0-31 | **质量检查写入硬编码假指标**，质量分不可信 | `quality_service.py:292-306,357-359` | 真实 SQL 聚合 |
| P0-32 | **复权未实现**：历史行情请求走全表扫描 + 复权缺失 | `handlers.py:1024` | 强制 ts_code/分页 + 复权 |
| P0-33 | **DataManager 整链死代码**：Clean/Quality/Research 引擎 + 流水线无实例化点（同步后质量检查靠 handler 直调 service 兜底） | `data_manager.py:57` | 删除或接入 initialize |
| P0-34 | **清洗空转假报成功**（规则映射零匹配）；**引擎队列任务必 ValueError**（DataType 枚举错配）；**质量引擎订阅字符串不匹配永不触发** | `clean_engine.py:1560-1567`、`sync_engine.py:583-584`、`quality_engine.py:348-354` | 统一枚举/映射/事件类型 |
| P0-35 | **stock_minutes 无唯一约束** → 重复同步产生重复分钟数据 | `create_table.sql:2822-2834` | 补唯一索引 |
| P0-36 | **日终 9 类型串行同步数小时**；仪表盘每次请求 8 个 DB session | `main_engine.py:230-256`、`dashboard_service.py:193-211` | batch 并发 + 单 session 缓存 |
| P0-37 | 因子分位在 EAV 表按宽表逻辑混算（结果错误）；主力资金榜用错日期基准恒空 | `stock_service.py:325-362`、`dashboard_service.py:101-118` | 按 factor_code 分组 / 独立查 MAX |

### 1.8 账户/绩效/监控/系统（来源：06 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-53 | 🔴 **JWT 密钥为公开占位符**，可伪造任意身份 token | `config_manager.py:114` | 强随机密钥环境注入 + 轮换 |
| P0-54 | 🔴 **登出无效**：主认证链不查黑名单 + logout 传 "unknown" → 注销后 token 仍可用 | `auth.py:58-255`、`system_router.py:518-523` | 黑名单接入主链 + 真实 token 登出 |
| P0-55 | **改密双重加密 → 下次登录必失败**（bcrypt 存储 + 再加密迁移分支） | `authentication.py:97-101`、`user_repo.py:106-111` | 统一 bcrypt，修正 migrated 分支 |
| P0-56 | **成交触发结算全失败**（结算事件日期 str → datetime.combine TypeError）；**结算事件双发**；**日终对账 100% 失败**（account.account_id AttributeError） | `settlement_engine.py:93,173-187`、`settlement_tasks.py:429-430`、`reconciliation_tasks.py:92` | 统一 date 类型 / 单一发布方 / 改 account.id |
| P0-57 | **初始资金双倍入账** | `account_manager.py:91-102` | 去重入金逻辑 |
| P0-58 | **告警去重失效 → 告警风暴**（hours=0 → created_at>=now）；**告警只落库不通知**（email 未注册）；metadata/metainfo 键名不符静默丢失 | `alert_service.py:95`、`alert_manager.py:52`、`monitor_alert_repo.py:72,134-138` | 修复去重语义 / 渠道求交集 / 统一列名 |
| P0-59 | **绩效口径分裂**：夏普三套公式（ddof/年化 rf）、回撤符号不一致致排名反转、年化 365.25 vs 252 三套、日盈亏含累计浮盈 | `pnl_calculator.py:182-195,355-368`、`financial_calculator.py:216-241` 等 | 统一 GIPS 口径（252 交易日、ddof=1、单日 mark-to-market） |
| P0-60 | token 有效期 30/600/60 三处不一致（实际生效 30）；种子 superadmin 明文密码 + DB 故障回退硬编码超管（fail-open） | `config.yaml:14`、`config_manager.py:115`、`constants.py:37`、`auth.py:101-145` | 单一配置源 + bcrypt 随机密码 + 503 兜底 |

### 1.9 共享层（来源：02 报告）

| # | 问题 | 位置 | 修复方案 |
|:---|:---|:---|:---|
| P0-47 | 🔴 **delete_by_time_range 的 where 结果被丢弃 → 无条件 DELETE 全表**（超表数据灾难级风险） | `hyper_repository_base.py:250` | `query = query.where(...)` + 回归测试，排查调用点 |
| P0-48 | 🔴 **RedisCache.set 把 pickle bytes 塞 JSON → 恒 TypeError**，Redis 启用后全链路缓存写入失败 | `redis_cache.py:68-73` | 整体 pickle/msgpack 序列化 entry |
| P0-49 | **SessionScope 对新建 CM 调 __aexit__ 会话泄漏**；**连接池 dispose 协程未 await 泄漏**；**死锁重试 rollback 整会话 → chunk 静默丢失** | `session_manager.py:146-152`、`connection_pool.py:150`、`repository_base.py:450-451` | 保存原 CM / sync dispose / savepoint 粒度重试 |
| P0-50 | **message_bus 响应队列名被丢弃 + reply_to 丢失 → request_response 恒超时** | `message_bus.py:189-192,294` | 统一队列名与消息头 |
| P0-51 | **路径穿越**：file_storage 未校验 `../`，可越权读写 | `file_storage.py:48-55` | realpath 前缀校验 |
| P0-52 | **一表一仓覆盖缺口**：9 张 SQL 表无 model/repo、3 组表名不一致（models vs SQL）、strategy_repo 绩效摘要返回写死 0 假数据、order_repo filters 被弃用过滤失效、**约 1500+ 行死代码**（messaging 全模块/QueryBuilder/cache_repo/distributed_lock 等） | 多处（见 02 报告 §2/§5） | 补建或标注废弃 + 统一表名 + 批量清理 |



---

## 二、死代码清单汇总（准备清理）

### 2.1 已确认死代码（05/07/09 报告）

| 位置 | 类型 | 清理建议 |
|:---|:---|:---|
| `utils/` 15 个文件（openapi/pagination_decorator/rate_limit/request_validator/data_sampler/log_context/log_formatter/structured_logger/optimization_tools/statistic_calculator/4×technical_indicators/time_converter） | 全库零引用 | 删除（rate_limit 建议接入而非删除） |
| `api/dependencies/auth.py:257-372` | PermissionRequired/require_permission/optional_auth 等无调用 | 删除或接入敏感端点 |
| `api/dependencies/database.py:128-333` | 13 个会话/事务函数无 API 层调用 | 收敛到 shared 层 |
| `api/dependencies/event_engine.py:44-71` | publish_system_event/EventEngineDep 无调用 | 删除 |
| `api/dependencies/config.py:22-31` | get_config_by_path | 删除 |
| `api/routers/__init__.py:42-57` | ROUTERS 列表 | 删除 |
| `api/routers/template_router.py` | 默认未注册（config 无 template 模块） | 与 strategy_router /templates 合并 |
| `basket_router.py:261-286` DELETE /batch | 被 /{basket_id} 遮蔽永不匹配 | 移到 /{basket_id} 前 |
| `strategy_router.py:987-1013` GET /feature-sets | 被 /{strategy_id} 遮蔽 | 移到前面 |
| `backtest_router.py:258` executor.cancel | 被注释成死代码 → 回测无法取消 | 还原代码（兼修功能） |
| `websocket/manager.py` _EVENT_CHANNEL_MAP 8 个旧事件名 | 与事件命名规范不符永不触发 | 与 modules events 收敛 |
| `response_formatter.py:560-618` ResponseMiddleware | 未注册中间件 | 注册或删除 |
| `xtp_adapter.py` | 构造 TypeError 且从未使用 | 修正或删除 mock |
| `strategy/events/signal_events.py` SignalExecutedEvent/SignalCancelledEvent 等 | （待 04 报告确认） | — |
| scripts/__pycache__ 源码已删的 pyc（backfill_new_factors、train_etf_bottom） | 残留 | 清理 |
| quant_web：layout.ts siderNavigation、IndustryMomentumChart/MacroTrendModal/FinStatementTable、getStockFactorScores、vite 临时文件、tmp_*.txt | 前端死代码/残留（08 报告将补充完整） | 清理 |

### 2.2 依赖层死代码

`pyqt5/qdarkstyle/qt-material`（桌面 GUI，Web 架构不需要）、matplotlib/seaborn/reportlab 待确认用途。

### 2.3 模块层死代码（01/02/03/04/06/08 报告）

| 位置 | 说明 | 清理建议 |
|:---|:---|:---|
| `core/exceptions/`（auth_exceptions/event_exceptions 等） | 约 3000+ 行异常体系死代码 + entities.py 实体死代码（01 报告） | 按引用清单删除/合并 |
| `shared/messaging/` 全模块 + QueryBuilder + cache_repo + distributed_lock + hyper_tables 管理类 + NestedTransaction/SessionScope/atomic | 约 1500+ 行零引用（02 报告） | 批量删除 |
| `data_manager.py` + CleanEngine/QualityEngine/ResearchEngine 三引擎 + `data/managers/` + `data/tasks/` 两包 + DailyStrategyRunner | 整链无实例化（03 报告） | 删除或接入 initialize |
| `strategy_loader.py` 整类 + `_predefined_paths` 指向已删目录；backtest_tasks/optimization_tasks 整类；data_loader stub；backtest_engine 旧接口兼容层 6 方法；task_manager/resource_manager 未接入；engine_factory 恒回退；portfolio_service 全 stub；parameter_validator 零调用 | 04 报告 | 批量删除 |
| 策略死分支：bottom_strategy `_on_bar_trace`/不可达分支；GlobalRotationV2 US 分支（US_PAIR=[]）；stock_low_high `_is_st_by_prefix`/`_stock_pool`/`_first_screen_done`/pump_dump 死分支/bear_max_pos 永不生效；P4 确认门槛形同虚设 | 04 报告 | 删除或经参数显式启用 |
| account/analysis：user_manager 登录锁定未接线、两套 JWT 实现、5 个 analyzer 死代码、monitor models.py 与 constants.py 枚举重复 | 06 报告 | 删除重复实现 |
| 前端：8 个视图无路由 + ~50 组件无引用 + 6 个 store 模块 + persistedstate.ts + StrategyLayout + locales + DashboardCard.vue（构建会失败） | 08 报告 | 按 08 §2 清单批量清理（可减约 30% 源码体积） |

---

## 三、跨报告问题统计（9/9 完成）

| 报告 | 状态 | 严重 | 高 | 中 | 低 | 小计 |
|:---|:---|:---|:---|:---|:---|:---|
| 01 core | ✅ | 0 | 10 | 10 | 0 | 20 |
| 02 shared | ✅ | 2 | 16 | 2 | 0 | 20 |
| 03 data+market | ✅ | 0 | 20 | ~30 | — | 50 |
| 04 strategy+backtest | ✅ | 0 | 14 | 6 | 0 | 20 |
| 05 trade+risk | ✅ | 0 | 10 | 10 | 2 | 22 |
| 06 account系 | ✅ | 0 | 20 | 若干 | — | 20+ |
| 07 api | ✅ | 0 | 15 | 9 | 6 | 30 |
| 08 前端 | ✅ | 0 | 4 | 14 | 2 | 20 |
| 09 全局/工程 | ✅ | 0 | 3 | 6 | 2 | 11 |
| **合计** | 9/9 完成 | **2** | **112** | **97** | **12** | **223+** |

---

## 四、修复路线图建议（定稿）

| 批次 | 内容 | 依据 |
|:---|:---|:---|
| **批次 1（数据灾难防护）** | 🔴 02：delete_by_time_range 全表删除、RedisCache 序列化；03：parent_task_id 缺列、PIT 停牌假行情、后复权公式、stock_minutes 唯一索引 | 数据安全第一，防不可逆损失 |
| **批次 2（资金与交易正确性）** | 05：成交落库、资金校验、风控阈值统一、涨跌停校验、SIMULATED_TRADING 防切换、半自动确认闭环、假执行删除、pnl_outcome 回填、双 RiskEngine 合并；04：同日超卖、T+1、NaN 防护、回测可复现、佣金配置生效 | 交易正确性 |
| **批次 3（认证与安全）** | 06：JWT 密钥随机化、登出黑名单接入、改密修复、AUTH_ENABLED 生产强制；07：信号确认/WS/因子定义加认证、异常信息脱敏、run-scenario 沙箱、logout 修复；04：exec 沙箱收紧、training_service SQL 注入；02：路径穿越校验 | OWASP |
| **批次 4（业务闭环修复）** | 06：结算 TypeError、对账修复、初始资金双倍、告警去重/渠道、绩效口径统一（GIPS）；01：事件优先级/async await/publish 修复、工厂死锁；08：WS 消息路由修复、401 处理、假订单删除 | 闭环正确性 |
| **批次 5（性能）** | 03：日终批量并发、仪表盘单 session；07：async 端点同步阻塞后台化、WS 背压；01：队列水位监控；02：executemany 批量写入、N+1 修复 | 性能 |
| **批次 6（架构与质量）** | 09：core 反向依赖解耦；死代码批量清理（utils 15 文件 + messaging 1500 行 + 异常 3000 行 + 前端 30% + 各模块死分支）；4 套响应格式/错误码统一；测试补充（trade/risk/backtest 优先）；依赖升级（fastapi/pydantic）+ PyQt5 移除 | Code Health / Clean Architecture |

---

## 五、分报告索引

| 文件 | 大小 | 核心发现 |
|:---|:---|:---|
| `01-core引擎基座审查报告.md` | 53.7KB | 事件优先级反转、CRITICAL 被当 NORMAL 丢弃、async put 未 await、工厂死锁、3000+ 行异常死代码、错误码撞值 |
| `02-shared共享层审查报告.md` | 36.5KB | 🔴 delete 条件失效→全表删除、🔴 RedisCache 序列化恒失败、session/连接池泄漏、message_bus 恒超时、路径穿越、1500+ 行死代码 |
| `03-data-market数据行情审查报告.md` | 46.3KB | parent_task_id 缺列、PIT 停牌假行情、后复权公式错误、质量检查假指标、DataManager 整链死代码、日终串行数小时 |
| `04-strategy-backtest策略回测审查报告.md` | 35.4KB | exec 沙箱 RCE、幽灵绩效 -100%、优化恒 -inf、佣金配置被忽略、同日超卖、NaN 污染、回测不可复现、T+1 缺失 |
| `05-trade-risk交易风控审查报告.md` | 44.8KB | 资金三层防护实际 0 层、成交不落库、半自动闭环断裂、假执行、阈值宽松 3 倍、涨跌停不触发、双 RiskEngine、pnl_outcome 不存在 |
| `06-account-analysis-monitor-system审查报告.md` | 47.1KB | JWT 密钥占位符、登出无效、改密双重加密、结算 TypeError 全失败、对账 100% 失败、初始资金双倍、告警风暴、夏普三套公式 |
| `07-api层审查报告.md` | 46.3KB | 信号确认无认证、WS 无鉴权、logout 失效、4 套响应格式、被遮蔽路由、异常泄露、同步阻塞事件循环 |
| `08-前端quant_web审查报告.md` | 44.8KB | WS 推送 commit 到不存在 store、8 视图+50 组件+6 store 模块死代码、全量 ECharts、时区偏差 |
| `09-全局架构与工程卫生核查.md` | 10.0KB | core 反向依赖 modules、utils 15 死文件、8 模块零测试、PyQt5 残留、.env/dist 工程卫生 |

> 全部 9 份报告均已附业界标准对照（GCR/Clean Code/SOLID/Fowler/OWASP/领域规范），问题均带文件:行号实证，未修改任何业务代码。
